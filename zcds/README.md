# 占城大师（zcds）—— 后台自动挂机 v3.1

游戏窗口标题含「占城大师」，进程是 `WeChatAppEx.exe`（微信小游戏容器）。
路线：**不走 API**（mini-data 加密层没破，逆向结论见 `AUTOMATION_REPORT.md`；该报告属逆向资料，不入库），
改成「抓窗口截图 -> 点色指纹定页面 -> 点色决定动作 -> PostMessage 点击」(只有非读字不可的页才补一次 ROI OCR)，后台跑，不抢前台。

## 跑起来

```
cd C:\projects\wxgame\zcds
python -m pip install -r requirements.txt   # 首次装依赖(版本已实测锁定)
python auto_bot.py                          # 需要游戏窗口已经打开
```

前置条件：游戏窗口已经打开。找不到窗口时 `auto_bot.py` 会抛
`RuntimeError: 找不到游戏窗口, 请先打开 占城大师`。

> 机器人启动后每轮都会把窗口外框钉成 **552x1006**（点色指纹和点击坐标的标定基准），
> 最大化会先还原再改尺寸；不想动窗口加 `--no-resize`。

| 开关 | 作用 |
|---|---|
| `--dry-run` | 只打印行为不真点；配 `--max-steps 8` 就是"真机只读冒烟"，先看路由对不对再放它点 |
| `--max-steps N` | 跑满 N 轮退出（回归/取证用），0 = 不限 |
| `--shots N` | **取证：每轮把当前帧存成 `shots_live/dbg_<轮>_<页>_<时分秒>.png`**。要查"这一下点在哪""按钮上有没有价格"就靠它，不改动任何行为 |
| `--max-battles N` | 打满 N 场退出 |
| `--no-resize` | 不钉窗口尺寸（指纹会整片失效，一般别关） |
| `--no-low-cpu` / `--affinity N` | CPU 降载开关（把微信进程设低优先级 / 限核） |

## 离线验证（不用开游戏，只读 `shots/` 里已导出的 295 张标定语料 + 真机取证帧）

> **`shots/` 语料不入 git**（295 张 / 117.5 MB，截图顶部带账号昵称）。
> 所以 clone 之后本节命令会因为找不到语料而报错：需要在游戏里自己采集截图，
> 在 `tools/pick_print.py` 的 `LABELS` 里登记后 `pick --label` 采指纹。定页逻辑本身不受影响。

```
python -X utf8 test_print_route.py            # 10 项断言 / 295 张语料：路由回归([9]=宝箱码表 [10]=软命中门限)
python -X utf8 test_zero_ocr.py               # 定页面零 OCR: 295 语料 + 真机留出共 2259 帧(1964 留出) 全要点色定页, 且 OCR 模块没被 import
python -X utf8 test_price_guard.py            # 绝不替玩家点付费按钮: 18 张真机帧回放(帧表派生自 LABELS) + 合成帧 + 带内宝石判据
python -X utf8 tools\pick_print.py check      # 语料与 LABELS 是否对得上
python -X utf8 tools\pick_print.py verify     # 点色指纹逐张判定，应 295/295 通过（63 张按设计无指纹：other/arena/压暗引导帧/stuck 取证帧）
python -X utf8 tools\print_stats.py --dry-run # 重算各页指纹统计，应与注释逐字一致
python -X utf8 tools\live_audit.py            # 真机证据帧全量体检(2648 张): 指纹 vs 文件名标签对账, 冒出一条没登记的"认错页"就退 1 (见 COLORPRINT.md 33)
python -X utf8 test_cpu_offline.py            # 主循环离线集成 8 场景（需要 rapidocr）：25 帧只花 1 次 OCR（只剩"连着两帧零命中"那条兜底）
python -X utf8 test_act_zero_ocr.py           # 动作层零 OCR：result/chest_open/lobby/chest_info/levelup/hero_level 六页逐帧落点表，一调 OCR 当场炸
python -X utf8 test_battle_loop.py            # 连打 3 场：进战斗页必须清空已点格子
python -X utf8 test_chest_livelock.py         # 开箱死循环回归：付费格必须被拉黑, 大厅改打对战(8 段 22 项)
python -X utf8 test_side_page_escape.py        # 侧页逃逸回归：引导模态落点 + 兄弟页签点[战斗]回大厅，6 场景全程 OCR = 0
python -X utf8 test_price_class.py             # 战斗价签分类器：271 帧格子读数逐档核对（含被裁切的价签、左下角"镜头复位"必须判 cls=3）
python -X utf8 test_battle_rank.py             # 战斗出手次序：同一帧只点最高档；连点 4 轮按 50矿>50兵营>25；全拉黑才停手；TTL 到期解禁；第 [9] 段=白标签判据不许随地图底色漂移（§34）
python -X utf8 test_anchor_print.py            # 锚点相对点色回归：弹窗面板会浮动 -> 5 段断言（锚点组 / 异页 0.00 / 查找 6ms）
python -X utf8 test_ad_escape.py               # 广告页自救：黑屏连续 2 帧 -> arm 看广告窗口(一帧都不点), 放完才点色找右上[关闭]，全程零 OCR
python -X utf8 test_ad_watch.py                # 看广告自动领奖：黄色[领取]判据(17 正/218 反) + 药丸两把尺子(右边界缩窄/白像素变少) + 顶栏门闩 + _ad_tick 状态机 + step() 端到端零 OCR
python -X utf8 test_newcard.py               # 新卡展示页(第 15 页)：4 帧硬命中 + 与卡无关性(15 点逐位色差 0) + 异页零点击 + step() 真跑 + 830 帧只有这 4 张命中 + 卡页检测器翻帧盲区复现
python -X utf8 test_popup_escape.py            # 弹窗出口回归：9 段 —— 徽章帧四判据交叉 / 真跑 step() 零 OCR 点到徽章 / claim_popup 徽章优先 / diamond_popup 认不出什么都不点 / 全量帧里"谁都不认"<=3 / battle 左上红装饰不是徽章 / B 路卡页警报 / 运营弹窗盖住有指纹页也要两帧内点掉 X(§33.6)
python -X utf8 test_chest_unlock.py          # 大厅宝箱五态码表：有免费格只点免费格 / 一格免费都没有才试探点 `p` 格，花不花钱由面板宝石像素硬闸判（132 帧大厅 / 歧义秒不参与配对）
python -X utf8 test_fps_popup.py             # 帧率自适应弹窗(第 16 页)：10 帧硬命中 + 15 点全在面板内 + act 落点(275,643) + step() 真跑零 OCR + 2764 帧只命中 14 个同页路径 + 回执键三态 + 额度扣减
python -X utf8 test_dim_frame.py               # 压暗闸门：现场帧 battle 0/15 + 不许被任何页认领 + 295 语料x两档压暗 590 次判定误判 0
```

## 当前进度（2026-09-04 早晨复验：离线 **18 个回归脚本全绿** + 真机第 25 轮 **400 步 / 379 条路由 / 点色 100.00% / 真 OCR 0 条**（第 24 轮那条 OCR = `result` 指纹压在 MVP 角色动画上，已清零，见 §24）；
**大厅宝箱**「有免费格只点免费格 / 一格免费都没有时**试探点 `p` 格**，花不花钱交给面板的宝石像素硬闸」——真机第 26 轮 2/2 帧实测 `p` 面板 = 362 宝石像素、一分没花，见 §26）；**第 27 轮**在用户截图的同一形态 `ppap` 上复验 2/2 帧试探、资源栏紫宝石 306 → 307（只增不减），并且实测证明**[空槽位]那张蓝卡永远不会被误判成 `p`**，见 §28）；**第 36 轮**查清并修掉「bot 进程里屏幕通道整帧全黑」的根因(窗口站点访问掩码)，换来第三把尺子「通道分歧度」，广告窗口改 0.5s 密拍 + [领取]免费重试，并结案取证语料的「同秒双帧」污染(加单实例互斥量)，见 §30.8~30.12；**第 36b 轮**（同一批长跑的后半段 08:26~08:41）真机抓到一张**没标指纹的「帧率自适应」弹窗盖住结算页 → 原地空转 15 分钟 / 118 步零动作**，双保险修法 = 新页 `fps_popup`（纯点色关弹窗）+ 通用「回执键」兜底档，见 §31；**第 37 轮**（09:02~）真机抓到全屏过场动画**把整帧压暗**（mean 168.9 → 137.2）导致 battle 指纹短暂 15/15 → 0/15 —— 判定为**设计内盲区**：动画 6~9s 自消、压暗期间没有任何页来抢（全表最高分 0.07），而硬凑「压暗形态」会在别的帧制造假全中（比零命中危险得多），所以不建指纹，改加第 18 道闸门 `test_dim_frame.py`：295 语料 × 两档压暗 = 590 次判定**误判 0**、压暗后仍全中比例仅 9.8%，且两道"零 OCR 闸门"同步改口径：认错页零容忍、慢一次凭"OCR 复核回同一页"的凭证记账放行，见 §32/§32.5；**第 38 轮**用户报「战斗中不会点格子了」——根因是 `battle_scan.py` 的白标签掩膜用了**绝对亮度** `>205`，而六边形棋盘底色会随地图漂（2686 帧实测中位数 199~215），浅色地图把整块棋盘吞进掩膜 → 连通块超宽 → `scan_words` 丢词 → **全语料 44.7% 的战斗帧一个格子都扫不到**（真机 `在架` 从 avg 4.4~7.2 塌成 avg 1.0 / max 1）；改成**局部对比** `mn-medianBlur(mn,15) >= 25` + 亮盘中值底 >=208 时降门兜底 → 可点白标签 6769 → **12178（+80%）**、整帧扫不到 44.7% → **15.4%**（残留 405 帧逐张核对全是无黑描边的异形碎片 = 旧判据的假阳性），真机第 39 轮**整轮 1400 步 / 809 个战斗帧** `在架` avg **6.12** / max 23 / **零格帧 0**、档位分布 50:430 / 25:303 / 100:67 / 250:9 全按既定优先级、真 OCR 0 条、付费宝箱闸拦下 18 次一分没花；`test_battle_rank.py` 加第 [9] 段 8 个检查点（= 实测 11 条断言）（两张锚点帧 + 亮图 +20 仍 >=3 + 旧判据在亮图必须交 0 个 + 207 帧抽样倍率 >=1.4（实测 1.58 倍））钉死，代价是单帧 15ms → **40.6ms**（主循环 3s/步无所谓），见 §34

| 层 | 状态 | 证据 |
|---|---|---|
| 抓窗 + 后台点击 | 通 | `game_utils.py` 纯 ctypes（PrintWindow / PostMessage），不需要 pywin32；真机 31 ms/帧 |
| 点色指纹定页面 | **12/17 页**（另 1 页 `tab_other` **刻意不标**，见下） | `pick_print.py verify` 通过 **295 / 不通过 0**（**63 张**按设计无指纹：`other` 39（含 `stuck_*` 取证帧）+ `arena` 13 + 压暗引导帧 `lobby_dim` 11，它们改由**引导落点 / 弹窗关闭徽章 / 兄弟页签 / 侧页箭头 / 转场闸门**五条点色出口零 OCR 处理）；`test_zero_ocr.py` 在 566 帧上量出**需要 OCR 兜底的帧 = 0**；全 828 帧对账里四条出口**都不认**的只剩 **3 张**（`COLORPRINT.md` §20.4）；`test_print_route.py` 10 项断言全过 |
| 页面动作 `act()` | 15/15 页都有 | `ad_popup` / `claim_popup` / `diamond_popup` **判页**仍靠 OCR 关键词（没语料标不了），但**动作层已经零 OCR**：两页的默认出口都换成点色 `find_close_badge()`（§20），`diamond_popup` 旧版那句"认不出就盲点 (270,300) 遮罩"已删（认不出就什么都不点）；**放完广告的整黑尾帧**连 OCR 关键词都没了，由点色几何出口 `ad_close_pos()` 处理（§18，真机 12:31 实证已能自救）；**看广告期间广告页根本不需要定页**：一旦 arm 看广告窗口，主循环跳过路由，只在窗口里判"放完了"再点[关闭]（§19） |
| 动作层点色（零 OCR） | 9 页已切换 | `lobby` / `result` / `chest_open` / `chest_info` / `levelup` / `hero_level` / **`newcard`** / `tab_other`（+ 本来就是纯点色的 `battle`）整轮不跑 OCR；`test_act_zero_ocr.py` 逐帧落点锁死，判据表见 `COLORPRINT.md` §13；`lobby` 宝箱槽现为**五态码** `o`/`U`/`p`/`a`/`.`，可点集 = `o`+`U`，`p`（解锁要宝石）由卡片右上角红角标判出（§23）；**2026-09-03 深夜**：一格免费都没有时按 `CHEST_PROBE_P` **试探点一格 `p`**，由面板宝石像素硬闸决定点/关（真机 2/2 帧 = 362 付费 → 只关面板 + 拉黑 300s，见 §26） |
| 战斗自动化 | 通 | 白标签掩膜 = **局部对比**（`battle_scan.white_mask()`：`min通道>205` 且比 `medianBlur(15)` 的局部底色亮 25，另带「整盘没有可点标签 + 中位底色 >=208」时降门兜底，防浅色地图吞格子，§34）+ 价签点色分类（`cls` 25/50/100/250/3）+ 档位优先级 `rank_cell()`：**50矿 > 50兵营 > 50问号 > 25 > 100 > 250 > 认不出**；`CELL_COOLDOWN = 2.5s` 单格冷却、`CELL_RETRY = 25s` 拉黑，`act()` 遍历候选点第一个没被拉黑的（旧版只看 `clickable[0]`，第一名点过就整轮不动手 -> 真机连续 95s 一次没点）。`test_battle_loop.py` 连跑 3 场 OCR = 0，`test_price_class.py` / `test_battle_rank.py`（含第 [9] 段「白标签判据不许随底色漂移」）锁死；**新增 `tier_census()`：每行点格子日志尾巴给出 `在架N 可点M 50矿:1 50兵:2 25:1`，真机第 20 轮 289 次点格子**档位违规 0**（§22.1 / §22.2）** |
| 主循环 | 通 | `test_cpu_offline.py` 8 个场景 / 25 帧只花 1 次 OCR；进 battle 会清 `clicked_cells`（本轮起是 `{格子: 点击时刻}` 字典）；软命中、点色零命中都要连续 2 帧才动手；转场（白烟/黑屏）整帧跳过，但**黑屏连续 `BLANK_AD_AFTER = 2` 帧**就改判"放完的广告页" -> **arm 看广告窗口**（本轮改：不再立刻点[关闭]，提前点 = 奖励作废 + 弹挽留框，§19） |
| 窗口尺寸对齐 | 通 | `--no-resize` 可关；尺寸不对时指纹层整片失效（实测 431x788 只剩 23/60） |
| 不花钱护栏 | 通 | `test_price_guard.py` 39 项；主判据是**按钮内下半那条带里的紫宝石像素数**（纯点色，免费 0 / 付费 362，阈值 `GEM_MIN = 60`），OCR 只有否决权、永远不能放行点；18 张真机帧的回放表**派生自 `LABELS`**（文件名带 `paid` 即付费帧），多采一轮真机不会让回归假失败 |
| 零 OCR 逃逸出口（引导 / **徽章** / 页签 / 箭头） | 通 | `step()` 的判据链 `guide_targets()` -> **`find_close_badge()`** -> `nav_present()`+`nav_tab_cx()` -> `back_arrow_pos()`：谁都不认才允许花一次全图 OCR。兄弟页签一律点中间[战斗] `(276,950)` 回大厅；**模态弹窗**直接点右上角红底白叉（落点现算，**七个不同高度 172/197/224/235/361~370/461/545**，x 也在 452~471 之间飘）。`test_side_page_escape.py` 6 场景 + `test_popup_escape.py` 5 段全程 OCR = 0；徽章那条**真机 15:52:57 首次自动命中 (452,361)**：`unknown escape-badge 1.00` + `点候选 1/4` 一次即中，8s 后回大厅；**第 19 轮 400 步又命中 4 次**（`hero_level` (469,172) + `vip_popup` (455,461)/(459,545)/(452,370)，全部第 1 候选即中、每次 3s 内回大厅，见 §21.8）。`COLORPRINT.md` §16 / §20 |
| 激励视频自动领奖 | 通 | 结算页黄色[领取]（`ad_claim_pos()` 纯点色，和[立即开箱]同一个黄、靠外接框形状分开）-> **看广告窗口**（期间不定页 / 零 OCR / 一帧都不点）-> 左上状态药丸**相对缩窄**判放完 -> 点[关闭] -> `done` 当帧交回路由点[继续]计一场。真机 300 步里连跑 4 次闭环（33/33/36/33 秒），全程 0 次 OCR；两次广告之间至少隔 `CLAIM_GAP = 120s`。`test_ad_watch.py` 4 段锁死，判据见 `COLORPRINT.md` §19；**「13:33 之后 0 次命中」已定案 = 每日 8 次额度用完**（横幅右下角本来就写「可用 N/8」，13:32:58 那帧是 1/8；`--ad-probe` 存的探针帧整带 0 个黄像素；全语料 38 张命中帧 = 33 失败 + 5 胜利 ⇒ 两种礼包共用同一颗黄色药丸），**判据不动**，见 §22.3 |

### 2026-09-03（深夜）真机第 25 轮 400 步**点色 100% / OCR 路由 0 条**复验 + 第 26 轮「无免费宝箱时**试探点 `p` 格**」

**A. 第 25 轮（`--max-steps 400`，20:32:37 → 21:05:30，日志 L9691..10424）= 上一轮那条 OCR 清零后的复验**
（统计脚本 `scratch/scripts/r26_stats.py 9691 R25 10424`）

| 项 | 实测 |
|---|---|
| 定页 | **379 条**：`print-order` 359 / `print-prefer` 17 / `print-anchor` 3 ⇒ **点色 100.00%** |
| 真 OCR 路由 | **0 条**（第 24 轮那唯一 1 条 = `result` 指纹站在 MVP 动画上，换点后不再出现） |
| 页命中 | `battle` 351 / `result` 8 / `lobby` 8 / `versus` 6 / `vip_popup` 4 / `matching` 2 |
| 转场 | `[兜底] 点色零命中` **14 次，全部是「第 1/2 帧」**（没有一次走到第 2 帧 = 无一处真卡死） |
| 战斗档位 | `cls=50` **133** / `cls=25` **125** / `cls=100` **13**（优先级 50矿>50兵营>50问号>25>100 按定案生效） |
| 异常 | `Traceback` **0** / `ERROR` **0**；动作层点色命中 16 次 |

**B. 第 26 轮（`--max-steps 80`，21:21:05 → 21:25:11，日志 L10425..）= 用户口径「宝箱下面写着[点击解锁]，你就点宝箱」落地**

- **改了什么**：`lobby.act()` 原来「一格免费都没有 -> 直接去打对战」；现在**先试探点一格 `p`**，
  免费/付费**交给 `chest_info` 面板的宝石像素硬闸**判（开关 `pages/lobby.py` 的 `CHEST_PROBE_P`）。
  付费 -> 只点右上角 X 关面板 + 拉黑该格 `CHEST_PAID_BLOCK = 300s`；**面板里只有那一颗黄按钮**，
  而 `chest_info` 必须「按钮带内宝石像素 < `GEM_MIN`」才肯点它 ⇒ 试探**不可能花掉宝石**。
- **真机时间线（第一次拿到「从 `p` 格点进去的面板」的直接证据）**：

  | 时刻 | 大厅状态码 | 动作 | 面板回话 |
  |---|---|---|---|
  | 21:21:12 | `oUUU` | 点槽1 (109,855)（`o` 免费开） | 21:21:15 进 `chest_open` |
  | 21:21:21 | `.UUU` | 点槽3 (331,855)（`U` 免费解锁） | 21:21:24 `点色命中免费按钮 (275,795) 带内宝石像素 **0**` |
  | 21:21:27 | `.pap` | **无免费格 -> 试探点 `p` 格 (220,855)** | 21:21:30 `按钮带内宝石像素 **362**>=60 = 要花钱` -> 拉黑 `chest@220,855` 300s -> 关 X (468,167) |
  | 21:21:33 | `.pap` | 节流中（`CHEST_PROBE_GAP = 25s`）-> 老实去打对战 (180,675) | — |
  | 21:24:05 | `ppap` | 试探**下一格** `p` (441,855) | 21:24:08 又是 **362** -> 拉黑 -> 关面板 -> 打对战 |

  ⇒ **`p` 格 = 只能花宝石** 这条判据从「语料间接推」升级为「真机点进去看过」（2/2 帧都是 362）。
- **整轮账**：定页 **76 条**（`print-order` 62 / `print-prefer` 14）⇒ 点色 **100.00%** / OCR **0 条**；
  页命中 `battle` 60 / `lobby` 6 / `chest_info` 3 / `result` 2 / `chest_open` 2 / `versus` 1 / `matching` 1 / `vip_popup` 1；
  兜底零命中 3 次全是第 1 帧；档位 `cls=50` 26 / `cls=25` 24；`Traceback` 0 / `ERROR` 0。
- **顶栏对账（证明没花宝石）**：20:54 大厅帧 木材 **1566** / 紫宝石 **303** / 金币 **7572**
  → 21:28:06 大厅帧（`scratch/shots_probe/lobby_after_r26.png`）木材 **1811** / 紫宝石 **306** / 金币 **7762**
  ⇒ 宝石 **+3**（战斗奖励），两次付费面板试探**一分没花**。
- **回归锁**：`test_chest_unlock.py` [4] 改成钉新行为（真语料 4 帧 + 合成状态矩阵 6 例：
  有 `U`/`o` 时**绝不点 `p`**、只有 `a`/`.` 时**不许试探**、整行拉黑时**连试探都不许**）；
  `test_act_zero_ocr.py` [4] 落点真值表同步（`live_lobby` `pppa` → (109,855)、`lobby_mixed_0019` `ap..` → (220,855)）；
  全 16 个回归 + `pick_print verify` **261/261** 复跑全绿。
- 判据细节与「为什么这是零风险」见 `COLORPRINT.md` **§26**；上一轮遗留的指纹漂移审计见 **§27**。

### 2026-09-04（早晨）真机第 36 轮：**屏幕通道全黑的根因是窗口站点掩码** + 第三把尺子「通道分歧度」

用户「运行起来，你看好，有 bug 随时修」。R35 那批 `ads_*.png` 全是 **1693 字节纯黑**，而同一时刻
独立进程抓同一块屏幕 `mean≈146` —— 单进程矩阵实测发现**决定性变量只有一个**：

| `OpenWindowStationW` 掩码 | `capture_screen` | PrintWindow |
|---|---|---|
| `0x0001`（旧代码只申请 READATTRIBUTES） | **0.0 整帧全黑** | 正常 |
| `0x037F` WINSTA_ALL_ACCESS | 142~146 正常 | 正常 |

低权限站点句柄会**废掉本进程的屏幕 DC**，而 `PrintWindow` / `EnumWindows` / `PostMessage` 全都照常，
症状才那么奇怪；更阴的是坏状态下 `GetDeviceCaps(HORZRES)` 仍返回 1920，**不能拿它自检**。现在
`enter_default_desktop()` 先自查（`EnumWindows` 数带标题的可见顶层窗口，已在交互桌面就不切），
必须切时用 `0x037F`；`ensure_window()` 里再加一道 `_screen_selftest()`，全黑就直接关掉屏幕通道。

**屏幕通道一修好，30.7 埋的那句「两条通道一致才敢判没广告」就能用了**：新增 `screen_div_frac()`
（逐像素最大通道差 >40 的比例），**无广告基线只有 4.1~5.2%**（差的就是顶栏 chrome + 抗锯齿），
而真广告帧 PrintWindow 那边 95% 纯黑、屏幕那边是视频 ⇒ 分歧度必然 ~95%，**阈值 0.25 落在正中间**。
接在 `_ad_tick` 撒手/重试**之前**：分歧度大 = 屏幕上确实盖着一层 PrintWindow 拍不到的东西 = 广告正在放
→ 继续等、一个像素都不点（否则就是自己把广告打断）。只在准备动手的那一帧抓一次（~25ms），不进常规密拍。

另外两处提速/体检：广告窗口改 **0.5s 密拍**（单帧 37ms，只吃单核 ~7%，所有判据都按秒算所以不会提前点[关闭]）；
[领取]等满 25s 没反应时**免费再点一次**（预算 1 次，用完或按钮不见了就照常撒手，不许死等）。
真机首批数据（07:43 起三次尝试）：第一次真把重试触发了，两批同刻帧分歧度都是 4~5% ⇒
**确认仍是真无填充**（游戏直接弹 ¥68 月卡礼包），广告额度「可用 6/8」一次没消耗、一分没花。

最后一件是**语料卫生**：`test_chest_unlock` 冒出一条"付费面板但大厅帧没有 p 格"的反例，
逐帧查下来判据没错 —— 是 09-03 07:48 **我同时开了两个 bot**，同一秒往 `shots_live/` 写了两张
状态码不同的大厅帧（`UoUU` / `ppap`），配对逻辑同秒平票按字典序拿了另一进程那张。
现在测试里**同秒状态码冲突的秒整秒不参与配对**（`n_amb=1 / n_amb_skip=2`，反例归零、
`n_free=10 / n_paid=57` 仍然够），并在 `auto_bot` 加了命名互斥量 `Local\ZCDS_AutoBot`
拒绝第二个实例（`--dry-run` / `--allow-multi` 才放行），见 §30.12。

### 2026-09-04（早晨）真机第 36b 轮：一张**没标指纹的弹窗**让 bot 原地空转 15 分钟 → 新页 `fps_popup` + 通用「回执键」兜底

用户「运行起来，你看好，有 bug 随时修」。R36 那次长跑（07:59 启动 `--max-steps 1400`）跑到 08:25 之后**连续 118 步零动作**，
08:26:30 起日志每 8 秒一条 `[兜底] OCR 连续 3 帧认出页面却零动作` + `[指纹] unknown escape-ocrzero 1.00`，一直到 08:41:49 我手工点掉为止。
现场是一张「**帧率自适应 / 已开启自动帧率控制…**」蓝面板 + 黄色[知道了]，**正盖在结算页上**（底下还看得见 [点击继续] 和 [失败礼包][领取] 可用 6/8）。

- **为什么不是"等一会儿就好"**：战斗中掉帧触发，弹窗**不会自己消失**、也不吃返回，只能点[知道了]。
- **死循环链条（关键，别忘）**：这页当时没指纹 → 点色整表零命中 → 允许花一次全图 OCR → **OCR 读到的是弹窗缝隙里的背景 `result`**
  → 判成 result → `ResultPage.act()` 的亮紫按钮被弹窗挡住 → return False → 连续 3 帧 → 交 unknown → 遮罩试探三点
  `(270,860)/(270,300)/(30,300)` 全落在弹窗外面 → 回到第一步。
  ⇒ **只要有一张没标指纹的弹窗盖在已识别页上，这条链就会重现**（和 §21 的 `newcard` 卡死 60s 是同一类病）。
- **手工解卡顺带把动作层标定出来**：`color_bbox(im, (190,595,360,695), 0xFDCA33, 85, 900)` → `(275, 644, n=3737)` → 后台点击，2.5s 后 bot 自己走掉。
- **修法一 = 新页 `pages/fps_popup.py`**：24 帧现场去重留 **10 张**语料，标定域**严格卡在面板内部** `--region 65,325,487,705`
  （面板外是被压暗的上一页，底色随 result/lobby/battle 变，混进指纹就等于"只在见过的那张背景上才认得出"）
  ⇒ **1 形态 × 3 个十字单元 = 15 点**，本页 **10/10 全中** / 异页 **0 误中**（异页最高只中 3/15）/ margin **0.20**；
  动作层不写死坐标，`BTN_BOX` 内现算黄色块外接框中心。注册顺序 `vip_popup > **fps_popup** > result`，必须压住 `result` 一头。
  ⚠️ 弹窗类用 `pick`，**不能用 `stable`**：`stable` 要求逐单元跨帧钉死 + 异页零误中，纯色大面板被 flat-color 卡到 **0 单元**（实测 0/10070）。
- **修法二 = 通用「回执键」兜底档**（防下一张还没标指纹的弹窗）：`auto_bot.py` 新增 `MODAL_OK_KW / _BAD / _MAX_LEN / _TRIES`，
  在**已经花掉的那次全图 OCR**结果里取**最后一个**「知道了 / 我已知晓 / 好的 / 明白」命中框（>6 字当正文；含「购买/充值/宝石/钻石/领取/广告/解锁/元¥$」一律拒），
  `unknown.act()` 在原有遮罩试探**之前**先点它，同一落点最多 3 次、落点变了才重新给额度 ⇒ **下次三步内自救**，不再空转 15 分钟。
- **护栏**：新回归锁 `test_fps_popup.py` 7 段，含 **2764 帧全量扫描只命中 14 个同页路径、零越界**、真跑 `App.step()` **全程 OCR 0 次**、
  回执键三态（正例 / 含"领取·宝石"拒 / 正文"我知道了"拒）、额度扣减与归零后交回原试探表；
  `runall` 16 → **17 项全绿**，`pick_print verify` 285 → **295/295**。
- 细节见 `COLORPRINT.md` **§31**。同轮还结掉 §27 挂着的 `matching` 漂移项：**不重标**（补帧后逐单元扫描凑不出可用指纹，且该页 `act()` 恒 False 不会卡死），见 §31.8。

### 2026-09-04（早晨）真机第 37 轮：全屏过场动画**把整帧压暗** ⇒ battle 指纹 15/15 → 0/15（判定为设计内盲区，不硬凑形态，改加第 18 道「压暗闸门」）

- **现场**：09:07:04 / 09:07:07 连续两帧 `[兜底] 点色零命中(上一帧=battle)`，09:07:10 允许花一次全图 OCR（675ms）→
  `[指纹] battle ocr 0.90` 认回本页并照常点格子 `(347,732) ore cls=50`，09:07:13 指纹自动恢复。截图 = 战斗里蹦出
  「**传说卡牌来袭！5% 超幸运！**」全屏过场动画（大法师 + 红色斜光带 + 黄色横幅），它**盖住整屏并把每一像素压暗**：
  全帧 mean 168.9 → 137.2，蓝推条 `(29,139,210)→(30,114,167)`、右下聊天气泡 `(249,254,254)→(213,217,217)`
  ⇒ battle 的 3 个十字单元（推条 ×2 + 气泡）**同时全灭**。现场帧 `shots_live/ocr_battle_090710.png`。
- **判定 = 不修**（和 §31 那张帧率弹窗的关键区别）：帧率弹窗是"不点就走不掉"的稳定页，必须建指纹；过场动画**放完自己消失**
  （实测 6~9s），代价只有一次 OCR 和约 6s 不出手。而硬凑一个"压暗形态"**反而危险** —— 压暗**不是等比缩放**
  （蓝条三通道系数 1.03/0.82/0.80，气泡 0.855/0.854/0.854，两组不一致），且动画本身在动（横幅在扫、光带在移），
  任何一组固定坐标只能蒙中某一相位 ⇒ **凑出来的形态会在别的帧制造"假全中"，假全中会点错按钮，比零命中危险得多**。
  安全性有实测证据：这张压暗帧上**全表最高分只有 `newcard 0.07`**（够不到软命中门限 0.20）⇒ 没有任何页来抢，
  交 OCR 是唯一出路，也是正确出路。
- **护栏**：新回归锁 `test_dim_frame.py`（第 18 项）把"压暗**不许改变判页结论**"钉死 ——
  [1] 现场帧 battle 必须 0/15 + 对照帧必须 15/15 + 亮度差 ≥20；[2] 压暗帧 `match_print` 必须 `None` 且最高分 <0.20（实测 0.07）；
  [3] **295 张语料 × 压暗 0.82 / 0.70 两档 = 590 次判定**，结论只许"同一页"或"无人认领"，**误判成别的页 = 0**（仍判对 58 / 无人认领 532）；
  [4] 压暗后仍全中的比例 ≤60%，实测 **9.8%**。⇒ 顺带得到一个体检数：把画面整体调暗 18%/30% 后指纹存活率只有 9.8%，
  说明现有 12 页指纹是在认"颜色结构"而不是"平均亮度"，而 `result` 那种本来就暗的页（mean 76.0）也不会因为更暗被认成别的页。
  细节见 `COLORPRINT.md` **§32**。
- **闸门改口径**（新增 `known_blind.py`，§32.5）：压暗帧进了留出语料后 `test_zero_ocr` 和 `test_popup_escape` 同时报红 —— 红得对，但**不去抬上界**（抬一次红一次，闸门就废了），改成按**失败性质**放行：**认成另一页 = 零容忍**（会点错按钮）；**谁都没认领 = 允许，但必须交出一条独立证据**：真机在同一秒还存了另一族同名证据帧（`dbg_NNN_<页>_<t>` <-> `ocr_<页>_<t>`）且**两张图逐字节相同** = OCR 当场复核回了同一页 ⇒ 身份没错、只是慢一次；再加盲区张数上界（有指纹帧数 x 0.5%，下限 4）兜量。实测 `battle 1970/1971 -> 1971/1971 + 压暗盲区 1 张(上界 12, 占有指纹帧 0.040%)`、`popup_escape [5] 上界仍是 3`（3354 张里还是那 3 张老帧）。**顺带废掉一个想当然的判据**：想用「比该页语料均值暗多少」来认压暗是不成立的 —— battle 语料均值本身就是 128.6~160.7 的双峰（正常波动 32 灰阶），而压暗帧 137.2 只比均值暗 16.4。分类器自己也被 `test_dim_frame.py` 新增的第 [5] 段 10 条反向断言锁住（改松 = 立刻红）。
- **同轮真机（R37，09:02 起 `--max-steps 1400`）**：`battle → result → vip_popup → lobby → chest_info → lobby → versus → battle`
  闭环稳定；¥68 月卡弹窗每次走**关闭徽章**点色出口，零 OCR；09:02 / 09:05 / 09:08 三次点[领取]全部**无填充**
  （通道分歧度 4.8~5.1% < 阈值 25%）⇒ 撒手，额度未动、一分没花；战斗档位实测严格按 `50矿>50兵>50问>25>100`。
  本轮**唯一**那条 OCR 兜底帧就是上面这张压暗帧，属于设计内成本。
- **全量体检**（新增 `tools/live_audit.py`，§33）：闸门不吃 `ocr_*`（标签来自 OCR）和已在语料里的 `dbg_`，那就再来一个**不带筛选**的复核 —— `shots_live/` 里 bot 自己存的 **2648 张**逐张过指纹，实测 **一致 2573 / 设计内压暗盲区 2 / 已登记不一致 19 / 未登记（= 真认错页）0**，14.6 ms/帧（和真机 bot 并行跑）。那 19 张**逐张看过图，全部是文件名标签错、指纹对**：4 张 `ocr_result_*` 其实是新卡页（OCR 把底部的"点击继续"读成 result）、1 张是帧率弹窗盖在结算页上、1 张是英雄等级弹窗盖在宝箱页上、12 张是 lobby 盖着"秘境大冒险"运营弹窗被 OCR 读成 versus、1 张是「攻城重锤」全屏动画盖住 battle；另有 5 张老构建只能记 `unknown` 的帧今天被指纹认了出来。**顺着那 12 张脏标签复查了第 28 轮那次 4 分钟空转：拿原帧真跑 `App.step()`，三张全部第 2 帧就点掉弹窗 X、一帧 OCR 都没花 ⇒ 早就修好了，并补进 `test_popup_escape.py` 第 [9] 段钉住，见 §33.6。****结论：文件名标签不是真值，所以不抬闸门上界，改成"登记表 + 新增即红"。**

### 2026-09-04（凌晨）真机第 28~35 轮：**看广告整条链第一次真机跑通**（零 OCR / 零人工）

用户「运行起来，你看好，有 bug 随时修」。这一段专门追"结算页黄色[领取] → 自动看完广告 → 自动关"，
真机第 34 轮 04:22:11 拿到**第一条完整成功的链**：

| 时刻 | 事件 | 判据 |
|---|---|---|
| 04:22:11 | `[结算] 点色命中[领取](看广告) (434, 820)` | 黄色 `0xFDCA33` 外接框在 `AD_CLAIM_BOX` 内 |
| 04:22:14 | 存帧 `ad_042214_p.png`，**[关闭]徽章 (488,77) 命中** | 点色徽章四判据 |
| 04:22:20 | 药丸 `(211, 827, 32)` | 顶栏带内白像素右边界 |
| 04:22:23 / 36 | `播放中 12s / 24s：本轮不动作, 也不花 OCR` | 药丸右 211 → 204 |
| 04:22:45 | `看了 33s, 药丸缩窄 211->164 = 已获得奖励 -> 点[关闭] (488, 77)` | 缩窄 47 ≥ `AD_PILL_SHRINK=28` |
| 04:22:48 | `点[关闭]后 3s 已离开广告页` → `[结算] 点色命中继续按钮 (275, 899)` | 交回正常路由 |

**本轮查清的两件事（细节见 `COLORPRINT.md` §30）**

1. **真广告帧在 PrintWindow 里是"顶栏 + 全黑"**（6 帧实测非黑像素 **5.03~5.10%**）——
   激励视频是独立合成表面，PrintWindow 抓不到视频层。之前一直把"取证帧是游戏页"当成"抓不到"，
   其实那 7 帧（非黑 **86.02~96.71%**）是**广告真的没起来**。判据没受影响：药丸和[关闭]都在顶栏。
   ⇒ 新增黑屏档 `ad_black_frac()` / `AD_BLACK_MAX_FRAC = 0.25`（5% 与 86% 之间两个数量级的空档），
   作用是给"广告刚盖上/正在加载"的过渡帧**免死 6s**，不是永久免检。
2. **"点[领取]没进广告"不是脚本问题**：新增 `focus_window()` 实测能抢前台（ChatGPT → 占城大师），
   而 04:16 那次点之前**已经在前台**；窗口 Z 序第 1、`WindowFromPoint` 五个采样点全命中游戏窗口、
   礼包页右下角「可用 7/8」额度没动 ⇒ **焦点/遮挡/坐标/额度四条全部排除**，
   真因是**微信激励视频冷启动没填充**（无填充时游戏直接弹「¥68 跳过激励视频」月卡弹窗，
   `vip_popup` 路由 + 点色关闭徽章已经兜住，不卡流程）。

**顺带修掉的老 bug**：`_ad_sample` 无条件写 `shots_live/` ⇒ 离线回归每跑一次就往真机取证目录丢假广告帧，
现抄 `_flag_stuck` 的口径（dry_run 走 `scratch/reg_stuck/`）。
**新增第二通道** `game_utils.capture_screen()`（`BitBlt` 屏幕像素，实测与 PrintWindow 同刻差异 >40 的像素仅 5.13%）
+ 取证密拍（arm 后 0.5s×9，专抓一闪而过的 toast），第 35 轮起生效。

### 2026-09-03（深夜）真机第 27 轮：用户截图「第 1 格空槽位 + 三张[点击解锁]」逐槽对账 —— 点，且只点宝箱

用户丢来一张大厅宝箱行的截图（第 1 格 = 蓝底白字[空槽位]，第 2~4 格 = 宝箱 + 底下[点击解锁]），
问的其实是两件事：**这种形态脚本到底点不点宝箱**、以及**会不会把空槽位也点了**。
真机第 27 轮（`--max-steps 90`，21:40:51 → 21:45:30，日志 L10599..10773）当场就是这个形态：

| 项 | 实测 |
|---|---|
| 大厅状态码 | `ppap` × 2 帧（三张[点击解锁] + 一张 [[AD]-30分钟]，与截图同一类） |
| 试探点 `p` | **2 次**：21:40:51 → (109,855)、21:42:41 → (441,855)（轮换游标，不死点同一格） |
| 面板回话 | 按钮带内宝石像素 **366** / **362** ⇒ 都判付费 → 只点右上角 X 关面板 + 拉黑该格 300s |
| 之后 | 25s 节流 → 老实点[玩家对战] (180,675)，全程没有第二颗按钮可点 |
| 定页 | **85 条**（`print-order` 77 / `print-prefer` 8）⇒ 点色 **100.00%** / OCR 路由 **0 条** |
| 页命中 | `battle` 75 / `lobby` 4 / `chest_info` 2 / `matching` 2 / `result` 1 / `vip_popup` 1 |
| 转场 | 兜底点色零命中 3 次，**全是「第 1/2 帧」**（没有一次真卡死） |
| 战斗档位 | `cls=50` 26 / `cls=25` 29 / `cls=100` 2（50矿>50兵营>50问号>25>100 按定案生效） |
| 异常 | `Traceback` 0 / `ERROR` 0 |
| 资源栏对账 | 21:28:06 木材 1811 / 宝石 **306** / 金币 7762 → 21:46 木材 1811 / 宝石 **307** / 金币 7822 |

⇒ 第 26 + 27 轮合计 **4 次付费面板试探，宝石只增不减（+1 是战斗奖励）**，`p` 格试探的护栏在真机上第四次成立。

**空槽位为什么不会被点**（`scratch/scripts/scan_brown.py`，语料 146 张 `lobby` 帧 / 509 个槽样本逐槽量）：
宝箱卡片本体画的是棕色宝箱，空槽位那张只有蓝底 + 白字，**卡身棕色像素恒为 0**：

| 状态码 | 槽样本数 | 卡身 (y765..812, cx±38) 棕色像素 min / 中位 / max |
|---|---|---|
| `.` 空槽位 | 276 | **0 / 0 / 0** |
| `o` [开启] | 76 | 335 / 385 / 1570 |
| `U` 免费解锁 | 134 | 367 / 1482 / 1660 |
| `p` 解锁要宝石 | 53 | 368 / 393 / 1659 |
| `a` [[AD]加速] | 45 | 367 / 392 / 1586 |

- [空槽位] 三个字实测落在 **y≈790..810**，在按钮带 `CHEST_BTN_BAND = (838, 882)` **上方 28px** ⇒
  白字判据读不到它，所以它天然是 `.`；语料里 90 张「≥2 个空槽」的 lobby 帧（例 `shots/after_battle_btn.png`
  四格全空槽 → `....`）逐槽复核，**无一误判**。
- 试探分支的前置条件是 `code == 'p'`，所以**空槽位连被点的资格都没有**。
- 上面那颗棕色像素**只当审计证据**用，代码里没再加第二道闸：0 / 335 已经分离到两个数量级，
  多一个阈值就多一处会腐烂的地方（判据细节见 `COLORPRINT.md` §28）。

### 2026-09-03（晚）真机第 24 轮 400 步：唯一那条 OCR 路由 = `result` 指纹站在**角色动画**上（已清零）

用户「游戏开着，你继续跑就是了」——本轮 `auto_bot.py --max-steps 400` 全程无人干预，20:11:46 → 20:32:37。

| 项 | 实测 |
|---|---|
| 定页次数 / 点色占比 | **382 条路由 / 99.5% 点色**（352 print-order + 27 print-prefer + 1 print-anchor） |
| 真 OCR | **1 条** —— `20:14:49 [指纹] result ocr 0.95`，**就是本轮修掉的那条** |
| 页命中 | `battle` 345 / `lobby` 10 / `result` 8 / `matching` 5 / `chest_open` 4 / `vip_popup` 3 / `versus` 3 / `levelup` 1 / `hero_level` 1 / `chest_info` 1 |
| 场次 | **8 场**（累计计数与日志一致） |
| 异常 | `Traceback` **0** / `ERROR` **0** / 拉黑 **0** / 白跑 **0** |
| 转场 | `[兜底] 点色零命中` **14 次，全部是"第 1/2 帧"**（没有一次到第 2 帧 = 没有一处真卡死）+ `[转场] 白烟帧` 4 次 |
| 新增取证帧 | **1 张**（那张 OCR 帧，已登记成语料） |

- **战斗档位优先级按用户定案生效**（50矿 > 50兵营 > 50问号 > 25 > 100），8 场共开 **251 格**，日志逐格带盘点。
  按 `cls` 分：**50 → 137 次**（矿 25 / 兵营 84 / 认不出图标 15 / 空 13）、**25 → 96 次**、**100 → 18 次**（全是兵营）。
  开头就是铁证：`在架6 可点6 50矿:1 50兵:1 25:4` → **点的是 (283,700) 那个 50 矿**，不是 4 个 25；
  下一帧矿没了 `50兵:2 25:4` → 改点 50 兵。**先 50 矿、再 50 兵、才轮到 25**，正是用户要的次序（旧版"只开 25 就不开"没再出现）。
  🔴 注：日志第 3 列是 **icon**（图标分类器）而不是价钱，`unknown`/`None` 是它认不出图标的两种置信度，档位一律看 `cls`。
- **本轮修的这一条不是判据坏，是点站错了地方**：`result` 指纹 25 点里缺的 2 点全在十字单元 **(386~390, 144~148)**，
  那正是 MVP 页上方**会换的 3D 获胜角色 + 闪光特效**：23 帧语料里 13 帧是同一个骷髅，另外 9 帧已经偏到 `492D31`，全靠 `tolerance(85)=19` 盖着；真机这帧是红龙 + 一颗大闪光正好压在采样点上。
  unit0 换到 (26,192) `0x2D1613` 王座厅纯背景（**跨 23 帧逐通道偏差从 22 降到 3**），留出集 **54 帧全中**、异页 238 帧**零误中**、margin 持平 0.24。
- **语料 260 → 261**（新帧登记为 `shots/result_live201449.png`）、`result` 分组 22 → **23**、
  `verify` **通过 261 / 不通过 0**（未全中交 OCR 仍是 61，一张没变差）、`test_zero_ocr` **567 帧 / 需要 OCR 兜底的指纹页帧 0**。
- **动作层本来就是对的**：那帧没有礼包横幅，亮紫[继续]在 (275, 829)，正是 §22.4 的"无横幅版式"。已补进 `test_act_zero_ocr` 码表。
- 🔴 **刻意没有用 `pick --label result` 重标**：新帧进来后 23 帧被"角色长相"拆成 **8 个形态**（多数只剩 1 帧），
  照抄 `prints=(...)` 等于把噪声写进指纹。正确做法是**保留稳定单元、只替换失效的那一个**。
- 完整选点口径（为什么必须限定 `y>=140`、为什么不能用"单单元判别力"当筛子）与安全区实测见 `COLORPRINT.md` **§24**。


### 2026-09-03（晚）宝箱[点击解锁]拆成 **免费 `U` / 要宝石 `p`** —— 判据是卡片右上角那颗红角标（真机第 21 轮）

用户诉求：**「这里宝箱下面有点击解锁，你就点宝箱」**。旧版其实**看到**白字就点了，但真机第 20 轮
12 次进面板、12 次全判付费（`带内宝石像素 366`）、12 次拉黑，**一次都没解锁成功** ——
问题不在"没点"，而在"点的格子根本不免费"。

- **根因**：卡片底部那几个白字[点击解锁]**其实是两种动作**。免费的是 `[解锁 🕐50分]`（放一个宝箱 + 起 50 分钟倒计时），
  付费的是 `[开启 💎120]`（花紫宝石立刻开）。两种卡片的宝箱贴图、白字、时长角标**逐像素相同**，
  旧版"按钮行白 >=120"这一档天生分不开 —— 它是个**假档**。
- **唯一区别 = 卡片右上角的红色八边形感叹号 ❗**（`0xFF3B3B`，实测 `x=槽中心+36..+52` / `y735..748`，约 16x14），
  语义是"**这一格此刻能白嫖**"：`o`[开启] 也带它，`p`/`a`/`.` 一律没有。
- **新码表五态**：`o` 免费开 / `U` 免费解锁 / `p` 解锁要宝石 / `a` [[AD]加速] / `.` 空槽，可点集 = `o` + `U`。
- **实测（596 个逐槽观测，语料 = `shots/` 260 + `shots_live/` 581 里全部大厅形状帧）**：
  角标像素**完全两簇** —— `.` 288 / `a` 45 / `p` 53 **恒 0**，`o` 76 / `U` 134 **120~128**，
  `(0,60)` 区间内**一个骑墙值都没有**，阈值 `CHEST_BADGE_MIN = 60` 上下各留 2 倍余量。
- **面板配对交叉验证（决定性证据）**：把每张 `chest_info` 面板帧配到它前面最近（≤60s）的大厅帧，
  免费面板 9 个时间戳 / 付费面板 34 个时间戳，**反例 0** —— 新规则在语料上就能把第 20 轮那 12 次白跑全避开。
- 🔴 **必须 `degree=60` 不能沿用默认 90**：`tolerance(deg)=int((256-256*deg//100)//2)`，`degree=90` 时同一格只数到
  **48~55**（<60 -> 全判成付费、把免费格一起废掉），`degree=60` 才数到 120~128。容差窄到把自己判死的典型坑。
- **真机第 21 轮首命中（19:33:36）**：`.oUU 可开 3 格` -> `o` 格走开箱动画 -> `..UU` -> `U` 格
  `[开宝箱] 点色命中免费按钮 (275,795) 带内宝石像素 0`（**第一次真机证明"点了就真免费"**）-> 打对战。
  紧接的第 22 轮（240 步 / 12 分 17 秒）**232 条路由 100% 点色、零次进 `chest_info`、零次拉黑、真 OCR 0 条**，
  4 场战斗闭环、158 次点格子 —— 对照第 20 轮：12 次大厅路由 = 12 次进面板 + 12 次拉黑。
- **护栏只加不减**：`p` 进不了可点集，`chest_info` 的宝石像素判付费 + 300s 拉黑原封不动退化成**兜底**；
  `test_price_guard.py` 39 项仍全绿。
- 新增回归锁 `test_chest_unlock.py`（5 段）；`test_print_route.py` [9] / `test_act_zero_ocr.py` [4] 同步改掉 22 处 `u`；
  `test_chest_livelock.py` **不用改**（它的 `.U..` / `UUUU` 两帧在新规则下仍可点）。**16 个回归脚本全 exit=0**。
- 完整判据与数字见 `COLORPRINT.md` §23。

### 2026-09-03（傍晚 2）战斗档位盘点写进日志 + 广告「0 命中」定案（真机第 20 轮）

- **档位盘点写进日志**：`tier_census()` 给每行 `[战斗] 点格子` 尾巴加上 `| 在架N 可点M 50矿:1 50兵:2 25:1`，
  口径是"这一帧扫描器看到什么、优先级判定让点什么"，不是事后统计。真机 400 步 **289 次点格子 =
  50矿 33 / 50兵营 88 / 50问号 20 / 25 122 / 100 26 / 250 0，档位优先级违规 0 次**
  （250 那一档在架 5 个、全在冷却里 -> `可点` 0，所以从没点过，符合预期）。`test_battle_rank.py` 第 [8] 段 6 条断言锁死格式。
- **广告 0 命中 = 额度用完，不是漏检**：全天 `[领取]` 命中正好 **7 次**、最后一次 **13:32:58**，那帧横幅右下角写着
  「可用 1/8」（03:09 / 07:38 两帧都是 8/8）；新加的 `--ad-probe N` 把"判色没命中"的结算帧存下来，
  6 帧（3 失败 + 3 胜利）在 **±120 容差下黄像素 0 个** = 横幅整条没画，不是变灰；
  38 张命中帧 = **33 失败 + 5 胜利** ⇒ [胜利礼包]/[失败礼包] 共用同一颗药丸，"赢了所以没横幅"解释不成立。**判据不动。**
  可反驳预测：额度重置后第一次进结算页应重新打出 `[结算] 点色命中[领取](看广告) (412, 782)`（见待办 14）。
- **顺手修掉一条写反的注释**：结算页[继续]按钮中心 **有横幅时 (275, 911)、无横幅时 (275, 830)**
  （51 帧实测，差 81px），旧注释把两种版式说反了，`pages/result.py` 已改正，见 §22.4。
- **回归全绿**：15 个测试脚本全部 `exit=0`、`check` 未标注「无」、`verify` **通过 260 / 不通过 0**、
  `stuck_in_shots` 仍是 20；**语料零新增**（`shots_live/` 581、552x1006 帧 828）——即这一轮没有任何一页判色退化去靠 OCR 兜底。
- 完整数字与证据链见 `COLORPRINT.md` §22。

### 2026-09-03（傍晚）第 15 页 `newcard`（新卡展示页）+ 卡页检测器的**翻帧盲区**

真机第 18 轮（用户在场，`--max-steps 400`）最后 74 秒对着同一屏空转到步数用完。日志里那 **10 条 `result ocr`**
不是噪声，是一张**从没见过的页**：打完第 6 场 → `levelup` 点色领升级奖励 → 弹出**新卡展示页**
（顶部黄字「新卡」+ 中间卡牌美术「弩炮 · 稀有卡牌」+ 底部**白字**「点击继续」，**没有亮紫按钮**）。
旧链路每 6.7s 转一圈：点色零命中 → 2b 防抖白等一帧（`page=None`）→ 2c 全图 OCR 只读到「点击继续」→ 判成 `result` 0.95
→ `ResultPage.act()` 在 `CONTINUE_BOX` 里找 `#CC56FF` 亮紫按钮 → 找不到 → **`return False` 什么都不点**。

- **新增第 15 页 `pages/newcard.py`**，15 点 / 3 单元，`语料 4/4 全中 / 异页误中 0 / margin 0.33`；
  `act()` 用 `color_button` 现量白字中心 (276,864)，`CONTINUE_MIN_PX = 400`（实测新卡 938 / 真结算 187 / 6）。
- 🔴 **标定域必须钉死在底部 176 行**：全域标定挑出来的点含 (168,476)、(256,524)，正压在 `[远程][机械]` 兵种徽章
  和卡名「弩炮」笔画上 —— **换一张卡整片失效**。两条看着最像"新卡专属"的判据实测都不够格当**身份**：
  顶部黄字带 `n>=120` 的帧全语料有 **101 张**（新卡自己只排第 13），底部白字带 `n>0` 的有 **429/830 张**。
- **`detect()` 恒返回 0.0**：这屏没有稳定可读的文字，留 OCR 入口只会被「点击继续」骗成 `result`（就是上面死循环的第 3 步）。
- 🔴 **连带修掉一个真正的瞎点**：卡页检测器**连着 60s 零动作一次 `[卡页]` 警报都没发**。根因是 `_flag_stuck()` 旧版第一句
  `page is None 就清零`，而 2b 防抖每圈必然制造一个 `None` 帧 —— **是防抖自己把卡页检测打瞎了**。
  现在 `None` 帧沿用上一帧页名，再叠一把 `frame_sig`（28x51 灰度缩略）：**画面一变就重新计数**，
  所以转场白烟 / 激励视频不会误报，而真卡死（画面一动不动）一定数得到 20 帧。取证帧目录也按 `dry_run` 分流，离线不再污染 `shots_live/`。
- **回归**：新增 `test_newcard.py`（6 段，含 `_flag_stuck` 三种节奏的**行为级**复现），**15 个回归全绿**；
  语料 256 → **260**、全 552x1006 帧 820 → **828**、`verify` **260 / 不通过 0**（未全中交 OCR 仍 61），
  且 `badge_cross` 复跑证明"点色定不出页"仍是 **152 张、四条出口交叉表一格没动**。
- 判据、标定域对照实验、`_flag_stuck` 新算法见 `COLORPRINT.md` **§21**；同轮还补记了**弹窗关闭徽章真机首命中**（**§20.5**）。
- ✅ **真机第 19 轮首命中即闭环**：开局第 1 帧就是它 —— `newcard print-order 1.00` -> 点 (276,864)，落点与离线预测**逐字一致**；整轮 400 步 380 条路由**全部点色、真 OCR 0 条**、`[卡页]` 0（旧版会在这里空转到步数用完），见 `COLORPRINT.md` **§21.8**。

### 2026-09-03（下午晚些时候）第 4 条零 OCR 出口 = 弹窗关闭徽章 + 看广告第二把尺子 + 顶栏门闩

延续用户诉求「识别一律用点色，OCR 准确率太低而且速度太慢」，本轮把**最后两类白烧 OCR 的帧**也收进点色，
并给"看广告放完了没有"补了第二把尺子：

- **全语料对账**（`scratch/scripts/badge_cross_0903.py`，820 张 552x1006 帧）：点色定不出页的 **152 张**里，
  按判据链归并 = 引导 68 / 箭头 44 / 转场 19 / 页签 12 / **徽章 6** / **谁都不认 3**。
  新增的第 4 条出口认的就是那 6 帧（= 4 个不同弹窗：新手指南 / 符石详情 / 英灵召唤详情），
  且这 6 帧**没有一帧同时带引导/页签/箭头** -> 纯增量，对既有三条出口零影响。
- **`find_close_badge()` 现在有三处出口**：`vip_popup`/`chest_info` 的关闭键、`claim_popup`/`diamond_popup`
  的默认出口、`unknown` 的第 4 条零 OCR 出口，另外还是 `vip_popup` 锚点组的锚。
- **`unknown._candidates()`** 排队 `引导 -> 徽章 -> 箭头 -> 页签`；徽章落点 = 现算中心 +
  `BADGE_OFFSETS = [(0,0),(0,-6),(6,0),(-6,0)]`（本体 ~36x36，±2px 抖动来自挡住它的手指图标）。
  取证帧 `stuck_guide_* / stuck_badge_* / stuck_arrow_*` **离线一律写 `scratch/reg_stuck/`**，
  绝不再往语料 `shots/` 里丢（上一轮一次回归就把未标注列表从 12 涨到 17）。
- **看广告第二把尺子** `ad_pill_state()`：除右边界外再数带内**白像素总数**，比本场峰值少 >=15% 且连续 4s
  -> 也算放完。实拍 7 帧对照：同一场倒计时态 827/830/794（自身抖动 4.3%）、放奖态 606（**-27%**）；
  另一家 SDK 的放奖帧却是 **914**（比上面那场的倒计时还多）-> 和右边界一样只能比"本场峰值"，不能定绝对阈值。
- **顶栏门闩**：药丸**左边界**漂移超过 `AD_PILL_LEFT_TOL = 20` 就判"顶栏被广告画面盖住了"，
  那一帧两把尺子全部不许作数（实拍 `shots/watch_124707`：左边界 32 -> 222、白像素 157）。
  这个门限从 6 提到 20 是踩出来的：两家 SDK 的左边界天然差 **7px**（32 / 39），
  取 6 会把"换了个广告源"误判成"盖顶栏"，`test_ad_escape.py` 第 [4] 段当场炸 3 项。
- **播报行升级**：`[广告] 播放中 Ns ... (药丸 右=211 白=830/830 左=32)`，收手原因直接写进日志。
- **回归**：新增 `test_popup_escape.py`（5 段）+ 改 `test_ad_watch.py` / `test_act_zero_ocr.py`，
  现在 **14 个回归脚本全绿**；真机 `--max-steps 600` **545 条路由 = 544 点色 + 1 OCR**
  （那 1 条是 13:32:58 `result` 被动画盖住一个判色点，还白捡一张待标语料）。
- 判据链顺序、全部对账表、取证帧规则见 `COLORPRINT.md` **§20**；两把尺子与门闩见 **§19.3.1 / §19.3.2**。

### 2026-09-03（午后）看广告自动领奖：点黄色[领取] -> 自动看完 -> 自动关（全程零 OCR）

用户诉求：**「这个界面上点击黄色的看广告领取，可以看广告的地方就自动看完」**（附结算页截图）。
已实现并真机连跑 **4 次**完整闭环（13:05:31 / 13:08:13 / 13:12:21 / 13:16:52），整轮 `--max-steps 300` 跑完 **502 行日志：走 OCR 的路由 0 条 / `giveup` 0 / `Traceback` 0**：

- **点黄色[领取]**：`result.act()` 第 1 步换成 `ad_claim_pos()`（纯点色，~1ms），旧版"OCR 找『领取』二字"已删。
  这颗黄和[宝箱立即开箱]是**同一个 `0xFDCA33`**，只能靠外接框形状分开：领取是扁宽胶囊（实测 w 124~126 / h 32~38），
  开箱被面板挤成窄高块（w 58 / h 63）。815 帧全语料复扫：命中 46 帧**全部是 result 页**、中心恒 `(412,781)~(412,782)`、
  其余 769 帧 0 误命中。
- **看广告窗口**（`auto_bot.py`）：`start_ad_watch()` 一 arm，主循环**停止定页、停止 OCR、停止一切点击**，
  只由 `_ad_tick()` 管"什么时候才准点[关闭]"。窗口优先于路由，所以广告创意每帧都变也不影响定页。
- **判"放完了"**：左上状态药丸的右边界 `ad_pill_right()`，跟**本场最宽**比缩窄（真机实测 270 -> 209 才算放完；
  语料里同一句"已获得奖励"是 164、另一路广告是 209 -> **绝对阈值换广告 SDK 必翻车**）。
  兜底：`AD_WATCH_MIN = 12s` 之前绝不动手、等满 `AD_WATCH_MAX = 40s` 也当放完、`AD_WATCH_TOTAL = 100s` 防死等、
  4s 内认出的是"游戏自己的页"且始终没有广告顶栏 -> `giveup` 撒手（`_ad_page_hard()` 只做点色硬命中，不花 OCR）。
- **关完当帧交回**：`done` 不额外白等一轮，同一帧掉回正常路由 -> 立刻认出 `result` -> 点[继续] -> 计一场。
- **挽留框策略反转**（`pages/ad_popup.py`）：正在放 -> 什么都不点；弹"是否继续观看视频" -> 点**[继续观看]**（以前是点[放弃]）。
- **节奏**：两次看广告至少隔 `CLAIM_GAP = 120s`（游戏侧冷却远不止 2 分钟，连点必撞"稍后再试"），隔不够直接点[继续]且**不算一场**。
- 总开关 `config.WATCH_ADS = True`；设 False 立刻退回"不点[领取]、只点[继续]"。
- 判据表与全部实测数据见 `COLORPRINT.md` **§19**；同时补写了 **§17 战斗格子读价签**、**§18 黑屏广告页自救** 两节。

### 2026-09-03（中午）战斗只会开 25 的、开完就不开：档位优先级 + 停手修复 + 弹窗锚点点色 + 广告页自救

用户复跑时只提了一句话：**「现在战斗中只会开 25 的，开完就不开了」**。两件事都成立，而且是两个独立的坑：

1. **优先级**（上一轮已改，本轮真机复验）：`rank_cell()` 按 `50矿 > 50兵营 > 50问号 > 25 > 100 > 250 > 认不出` 排序。
   `test_battle_rank.py` 在 271 帧真机战斗语料上跑：186 帧有 50 矿、196 帧有 50 档，
   **这些帧全部先点 50**，一次都没被 25 抢走。真机 12:09 那段 48 次点击里 barracks50 已占 20 次。
2. **停手**（本轮修）：`CELL_COOLDOWN` 从 5s 降到 **2.5s**（battle 轮询 3.0s，旧值等于每两轮才出手），
   更要紧的是旧 `act()` 只看 `clickable[0]` —— 第一名一旦在冷却里就整轮 `return False`。
   真机 12:11:07~12:12:42 **连续 95 秒一次没点**，当时棋盘上还有 14 个白格。
   现在改成遍历候选、点第一个没被拉黑的，并加 `CELL_RETRY = 25s` 的到期解禁，日志多出
   `跳过已点N格` / `解禁N格` 两条可观测线索。回归第 [7] 段按档位连点 4 轮，全拉黑那一轮才允许不动手。

顺带清掉两处：

- **弹窗面板会浮动 -> 绝对点色不中**：`vip_popup` 真机判页仍要走一次 OCR。新增**锚点相对点色**
  （`anchor_prints` / `anchor_points`）：先按"徽章左上角"定位面板，再按固定偏移读那排金币点。
  `match_print()` 路由升到四级：**绝对全中 -> 锚点全中 -> 软命中 -> 全图 OCR**。
  665 帧审计：锚点只命中 vip_popup 1 帧、异页最高 0.00、查找 6.3ms。见 `COLORPRINT.md` §6.2。
- **放完的激励视频广告页永久卡死**：12:18 起整页黑，`is_transition()` 判成"黑屏转场"
  -> 每轮既不动作也不跑 OCR -> **31 帧 / 4 分钟干等**。根因是 `pages/ad_popup.py` 靠 OCR 关键词
  `放弃` / `秒后可获得奖励` 定页，广告放完后这两个词都没了。修法：黑屏连着
  `BLANK_AD_AFTER = 2` 帧还不走就不是转场，用 `ad_close_pos()`（顶栏右半边白团块 + 整页必须黑）
  点掉[关闭]。**真机 12:31:16 实证**：点 `(472,90)` 后 8 秒回到 `result` -> `vip_popup` -> `lobby`。

### 2026-09-03（上午）真机卡点在"回不到大厅"：新增新手引导落点出口 + 底部导航栏点色 + 新页 `tab_other`

用户在场复跑，能打的都通了，剩下的毛病全在**侧页**：一点进商店/卡牌/城堡/排行，或者被新手引导按在压暗层上，
脚本就"不认识自己"了 —— 那些页**根本没有返回箭头**，旧版 `unknown` 只能试箭头，试不出来就烧 OCR。
本轮定了一条更便宜的原则：**认不出页没关系，先认栏**。

- **底部导航栏判据**（`pages/base.py` 末尾，已进全语料验证）：`NAV_POINTS` 16 个在 5 个页签间**逐通道极差 <=6**
  的静态图标像素，`nav_hits() >= NAV_MIN_HIT(12)` 即"下面那排页签在屏幕上"。802 张 png 实测分界：
  **带栏 152 张最低 14 / 不带栏 650 张最高 9**（那 650 张全是压暗引导帧），门限上下各留 >=3 余量。
- **亮的是哪个页签**用`nav_tab_cx()`：选中页签背后那块**亮青高亮板的边框**会竖直穿过页签中心列
  （选中列 20~50 个青像素，未选中列恒 0）。⚠️ 中途试过"页签板左右角点"，**相邻页签板边框只差 9px 会歧义**，别再用。
  页签中心 x = 50 商店 / 163 卡牌 / **276 战斗（=大厅）** / 389 城堡 / 502 排行，y≈950。
- **新页 `pages/tab_other.py`（第 15 页）刻意 `points=()` / `prints=()` 不标身份**：兄弟页签是同一套壳换内容，
  标指纹等于给活动美术挖坑。它只由 `step()` 的"栏在 + 亮的不是中间"这一条**按上下文点名**，
  `act()` 就一件事：点 `(276, 950)` 回大厅，类级 `GAP=2.5s` 节流。为此新增 `App.page_by_name(name)`；
  注册顺序必须在 `versus` **之后、`unknown` 之前**（主循环拿 `self.pages[-1]` 当兜底页）。
  `test_print_route.py` 加了 `CONTEXT_PAGES = ('tab_other',)` 白名单，免得下次有人以为漏标。
- **为什么导航栏判据必须排在 `back_arrow_pos()` 之前**：真机实测**卡牌页**左下角那颗青色卡牌图标会被
  `back_arrow_pos()` 误认成返回箭头 `(84, 960)` —— 而它**正好是卡牌页签自己**，先走箭头逃逸就是原地打转。
- 🔴 **修掉一个真 bug：`step()` 里加出口绝不能提前 `return`。** 2b2 两条逃逸出口最初写成
  `return self.pages[-1], 0.0, False`，可 `page.act(self)` 在 **`step()` 函数尾部的公共代码**里 ->
  **`act()` 一帧都没执行**，日志却写着"交 unknown 处理"，看起来就是"一直在处理但手没出去"的死循环。
  改成给 `page, score, src` 赋值后落到尾巴；顺带两条：`self._nohit_streak` 停在 `NO_HIT_OCR_AFTER - 1`
  （清 0 会退化成"每隔一帧才动一次手"）、提前分支要补 `ocr_ran = False` 防 `UnboundLocalError`。
- **新手引导模态判据接上出口**（判据上一轮已标定：压暗层 + 手型指引，指尖 = 掌心 + `(-37,-24)`，1.8 ms；
  `cv2.connectedComponents` 那版 3.4 s/帧已弃用）：`auto_bot.py` 2b2 现在会 `guide_targets(img)` 拿落点交
  `unknown` 逐候选点，**认引导不花 OCR**；`unknown._candidates()` 末尾也加了"栏在且亮的不是中间"的兄弟页签兜底。
- **测试**：新增 `test_side_page_escape.py` 6 场景 / **全程 OCR = 0 次**（13 张引导帧判为模态且给出落点、
  6 张正常页不误判、4 个兄弟页签实测点 `(276,950)`、有箭头无栏的侧页零 OCR、卡牌页箭头误命中但路由先认栏、
  导航栏语料分界、unknown 同帧最多点 4 次就停手）。`test_cpu_offline.py` 场景 6 重写成
  "中性帧第 2 帧才允许花 1 次全图 OCR" + 6a 有箭头 / 6b 引导模态都零 OCR；`test_print_route.py` 放行 `CONTEXT_PAGES`。
  **离线 8 个回归全绿**。
- ✅ **真机两次实跑**（游戏开着，`WeChatAppEx.exe` 552x1006）：
  - 14 步短测：**从排名页（亮 x=502）自己点[战斗]爬回大厅** -> 开 2 个宝箱 -> 判出 2 格要花钱 -> 拉黑 -> 转打对战；13 次路由全点色。
  - 90 步长测：`大厅 -> 玩家对战 -> (白烟转场跳过) -> battle 逐格点色 -> 结算点继续（连打 2 场 + 第 3 场进行中收尾）
    -> vip_popup -> 大厅 -> 开箱` 闭环，**82 条 `[指纹]` 路由里只有 1 条走 OCR**（`vip_popup`，见待办第 7 条），
    stuck 0 / ERROR 0 / Traceback 0；21 次格子点击、2 条 `[结算]`、2 条 `escape-guide`。

### 2026-09-03（早晨）补第 12/13 页 `levelup`（升级领奖）/ `hero_level`（英雄等级面板）+ 卡页闸门

- **两页都是"真机审计 -> 现场复现"挖出来的**，不是照抄界面清单：140 步那轮日志里发现两类误判帧 ——
  `07:39` 结算页点完继续、关掉月卡弹窗后露出的**升级领奖页**，以及 `07:48:30` 被全图 OCR 判成
  `chest_info` 的**英雄等级面板**（`[指纹] chest_info ocr 1.20`，两页标题是同一张蓝横幅 + 白字）。
  后者的入口量出来是主页左上角那个等级六边形 `(30,113)` —— 点它就弹面板。
- **`levelup`（第 12 页）** 25 判色点，只标"升级"标题 + 黄色[领取]按钮两处**与等级/奖励内容无关**的恒定区域：
  `pick_print` 全图扫会把六边形里的等级数字"5"选进指纹，那样升一级整页指纹就废 ->
  改成分两段 `--region 0,350,552,430` / `--region 0,700,552,1006` 标定再合并复核。
  动作层的黄按钮和 `chest_info` 的开箱按钮**是同一个 `#FDCA33`**（付费面板点一次就花 30 紫宝石），
  所以加了两把尺子：中心 y 带 `(760,790)` + 像素数 `3000~6800`（实测本页 n=5524 / chest_info n=7655~8061 恒在带外）。
  真机跑通：`07:48:13 [升级] 点色命中领取按钮 (275, 776) n=5524` -> 后面接开箱→大厅→对战闭环。
- **`hero_level`（第 13 页）** 注册在 `chest_info` **之前**。面板主体（骑士立绘/六边形属性数字/底部奖励木箱）
  一升级就变，`probe_hero_diff.py` 拿相隔 11 分钟的两帧逐行比对：只有 y94~116（等级进度条 0/220→50/220）
  和 y879~956（底部导航角标）在动，**面板本体 y140~880 完全一致** -> 40 个判色点全部只标在标题横幅带 y160~190 内。
  动作层唯一动作是点右上角红底关闭徽章：实测本页恒 `(469,172) n=262`，而 `chest_info` 那颗**同款**徽章在
  `(468,159)`，只低 13px -> 中心 y 带取 `(160,190)`、像素数 `150~420`，越带/超限一律不动。
  真机首帧即命中：`08:14:23 [指纹] hero_level print-order 1.00 -> [英雄等级] 点色命中关闭徽章 (469, 172) n=262`
  -> `08:14:26 [状态] hero_level -> lobby`，全程一个都没读字。
- **标定方法学补一课**：`pick_print` 报的 margin 是**点级**重叠，两页横幅只差 ~10px 时它看不出问题
  （`hero_level` 直接选点最好也只有 0.28~0.50）。新增两个可复用工具：
  `scratch/scripts/pick_hero_units2.py`（逐十字单元：整单元在 188 张异页语料**零命中** + 点级重叠 <=阈值）
  和 `compose_hero.py`（按单元中心拼指纹，立刻全语料复核"本页全中/异页误中/margin"并输出可粘贴的 `points`）。
  本页最终 8 单元 40 点 / 5-5 全中 / 异页误中 0 / margin 0.100。
- **卡页闸门**（`auto_bot.py` 三个常量，都在文件头写明来历）：`SOFT_ACT_AFTER = 2` 软命中要连续 2 帧同页才动手、
  `NO_HIT_OCR_AFTER = 2` 点色零命中先老实等一帧再考虑花 OCR、`STUCK_AFTER = 20` 同一个"该动手"的页连着
  20 轮一次都没动就存 `shots_live/stuck_*.png` + WARNING（`battle`/`matching`/`versus`/`unknown`/`ad_popup`
  是合法的"等着不动手"页，不计）。闸门顺序：转场 -> 零命中先等 -> 才轮到 OCR。
- **语料 184 -> 193 张**（`levelup` 4 + `hero_level` 5），`pick_print verify` 193/193；
  真机留出 260 帧复跑"需要 OCR 兜底的指纹页帧"仍是 **0**。

### 2026-09-03（凌晨）真机跑通 PVP 全闭环：`chest_info` 纯点色 + `versus` 新页 + 转场闸门 + 开箱死循环拉黑 + 零命中先等一帧

用户在场开着游戏连跑三轮，最后一轮 `--max-steps 120` **全程零 OCR** 走完整个闭环：

```
chest_info(带内宝石 362 -> 付费) -> [拉黑] chest@all 300s -> [主页] 四格里能点的都在拉黑期
  -> 点色命中[玩家对战] (180,675) -> lobby -> versus -> [转场]白烟帧(不花 OCR)
  -> battle(3 分 06 秒, 逐格点色) -> [结算] 点 (275,910) 累计 1 场
  -> lobby -> versus -> battle ... -> 达到目标轮数 120, 结束
```

255 行日志 / 119 条 `[指纹]` 的审计：**含 `ocr` 字样的只有 1 行，且那行是转场闸门的文案**；
软命中 0 次、转移噪声 0 次、ERROR 0、Traceback 0。打赢一场（奖励 20 金 / 1 宝石 / 20 木 / 宝箱 x1）。
上一版的**开箱死循环已确认消除**。

- **修掉开箱死循环（跨页拉黑）**：真机在付费面板上「判付费 -> 只点关闭 -> 回大厅 -> 又点同一格」空转了 16 圈。
  现在 `App.block()` / `App.is_blocked()` 是一张带 TTL 的**跨页拉黑表**：面板判付费就按 `chest_slot_key`
  把那一格拉黑 `CHEST_PAID_BLOCK = 300` 秒（说不清是哪一格时退化成 `CHEST_BLOCK_ALL` 拉黑整行），
  大厅 `chest_states()` 把拉黑期内的格子标成不可点；整行都在拉黑期就改点[玩家对战]。
  **到期会自动回头再试**（宝箱会随时间转免费，不能永久封）。`test_chest_livelock.py` 8 段 22 项锁死。
- **新增 `versus` 页（对战列表）**：点[玩家对战]之后、匹配之前那一屏，以前只能算 `unknown`。
  6 个十字单元 / 30 判色点，注册在 `lobby` 之后。
- **`chest_info` 动作层去掉 OCR**：黄按钮中心恒 `(275,795)`；价格不看文字，只看按钮内下半那条带
  `GEM_BAND_BTN = (-50,0,50,40)` 里的**紫宝石像素数**（免费 0 / 付费 362，阈值 `GEM_MIN = 60`）。
  判付费就点右上角关闭徽章（`find_close_badge()` -> (468,165~167)），一次都不碰按钮。
- **点色零命中「先等一帧」再花 OCR**（`NO_HIT_OCR_AFTER = 2`）：真机 03:54 那帧被爆炸动画整片盖住，
  指纹零命中，旧版白烧 675 ms 全图 OCR 还判成 `unknown`。现在第 1 帧什么都不做也不跑 OCR，
  连着两帧零命中才兜底 —— 动画/转场帧天然被这道闸挡住。
- **语料 166 -> 184 张**，`pick_print verify` 184/184。真机**留出集** 203 帧（不参与标定）复跑：
  点色全中、判定 100% 正确，只有 3 帧需要兜底（2 张白烟 + 1 张爆炸帧），而这两类现在都被闸门挡住。
  `harvest_live.py` 也加了闸门：**本就没指纹的页（`other` / `unknown`）不再"必收"**，
  转场帧留在 `shots_live/` 当留出集，不污染标定语料。
- **真值帧「命名即身份」**：`chest_info_live040458.png` 人眼核对后确认是付费帧 -> 改名
  `chest_info_paid_040458.png` 并登记进 `LABELS`；`test_price_guard.py` 的真机帧表**改成从 `LABELS` 派生**
  （文件名带 `paid` 即付费帧），以后每采一轮真机，回归都不会假失败。

### 2026-09-03（晚）动作层也改成点色：大厅 / 结算 / 开箱**整轮零 OCR**

用户诉求原话：「识别成功以后记得在脚本中用点色来识别，OCR 准确率太低而且速度太慢」。
点色指纹定页面早已 100% 命中（128 张语料 + 47 张真机留出帧），但**定完页之后 `act()` 里还在读字**：
大厅每帧一次 ROI OCR 369ms，结算 / 开箱每帧一次全图 OCR 675ms。这一轮把三页的动作层也换成数颜色 ——
判据、阈值、实测区间全部写进 `COLORPRINT.md` §13。

- `result`：结算页整屏只有**一个**亮紫色块 `0xCC56FF`，取它外接框中心就是 [继续] 落点。
  顺带修掉一个老 bug：激励视频盖脸那帧（`watch_124707`）OCR 会把广告文案读成按钮然后瞎点一次，现在 n=0 -> 本帧不动作。
- `chest_open`：底部按钮文字是全屏唯一白色大块，[点击领取奖励] 与 [点击关闭] 的中心**完全重合**，一次取色通吃；
  左上 [跳过] 白块只在动画期存在，拿它当状态位，旧版第三个 OCR 分支（找「跳过」二字）一并删掉。
- `lobby`：四张宝箱卡片的按钮**颜色构成**不同（[开启] 纯金 / [[AD]加速] 金按钮旁贴蓝票券 / [点击解锁] 深底白字 / 空槽没按钮），
  数「金色被挡住多少」就能分四态，只点 `o`/`u`，[[AD]] 一律不点；[玩家对战] 改成认那块金色按钮 ——
  旧版靠 OCR 读「玩家对战」，**33 帧语料里一次都没读出来过**，等于这个按钮机器人从来没点着。
- 三页都声明 `act_needs_ocr = False` => 主循环**干脆不为它们跑 OCR**（`ctx.f` 只剩图，`act()` 只准用 `ctx.f.img`）。
- 新增 `test_act_zero_ocr.py`：桩里把 `ScreenFeature.find/find_boxes/has/near` 和 `ctx.need_text()` 全改成抛异常，
  动作层只要伸手动文字就当场炸；同时断言 result 16 帧 / chest_open 6 帧 / lobby 15 帧的**逐帧落点表**。
- `test_cpu_offline.py` 跟着改成「大厅·战斗·结算·开箱全程 `ocr=False`」：现在 **21 帧只跑 2 次 OCR**（旧版每帧一次 = 21 次），
  两次都留在 `chest_info` 的价格护栏上。

速度：点色判据整张表 2.2ms，比一次大厅 ROI OCR 快约 **167 倍**，而且不会像 OCR 那样把「继续」读成「维续」。

### 2026-09-03（下午）定页面彻底改成「点色优先」

用户定案：**OCR 准确率太低而且速度太慢 —— 识别成功以后，脚本里一律用点色**。实测差距：
点色判完全表 **1.2~2.2 ms/帧**，全图 OCR **675 ms/帧**（约 300~500 倍）。三条改动：

1. **留出集审计**（`scratch/scripts/audit_holdout.py`）：只拿**没参与标定**的 47 帧真机图测。
   结果 battle 19 / lobby 20 / chest_info 5 / chest_open 1 / matching 1 / result 1
   **点色全中 100%、页面判定 100% 正确、零 OCR** —— 上一轮重标定确实修好了真机失效，
   而不是只在语料上自证（这条很重要：以前两次"假绿灯"都是 `verify` 过了真机废）。
2. **软命中层**（`pages/base.py`，详见 `COLORPRINT.md` §6.1）：真机战斗页满屏都在动，
   指纹 25 点里被动画遮住 1 点就会"不全中" -> 旧版整轮白退到全图 OCR + 文字猜页。
   现在「差 ≤1 点、参评指纹 ≥8 点、甩开第二名 ≥0.20」算**软命中，依然不跑 OCR**；
   主循环再要求**连续 2 帧同一页**才按它动手（头一帧只认页不出手）。
   参数实验（`scratch/scripts/soft_tune.py`，222 帧）：`exact` 205 / 差1点 205 / 差2点 205 ——
   软命中一个误判都没多，所以取最保守的「差 1 点」。
3. **零 OCR 回归**（`test_zero_ocr.py`）：128 语料 + 47 留出帧逐张走 `route_prints()`，
   有指纹的页必须 100% 点色定页、无指纹的帧必须**不被乱认**，扫完断言
   `rapidocr` / `paddle` / `vision` 至今没进 `sys.modules`。平均 1.2 ms/帧。

### 2026-09-03 真机实测后修的六类问题（用户在场实测，日志 `bot.log`）

1. **弹窗关闭键是图形，OCR 读不到** -> 新增 `pages/base.py: find_close_badge()`，按颜色找"红底白叉"徽章：
   红 mask `(r>185)&(g<105)&(b<105)&(r-max(g,b)>90)` + 12px 网格 BFS 连通域，红像素 550~950、
   外接框 30~60、框内白像素 >=60，多候选取**最靠右**。106 帧 0 误报，单帧 15~20ms。
   旧逻辑找不到 `X` 就点遮罩，每轮点同一个 `(270,860)`，月卡弹窗 8 轮关不掉（dry-run 复现）。
   接入 `vip_popup` / `chest_info` / `diamond_popup` 三处；实测命中 `(455,461)`/`(468,165)`/`(465,235)`。
2. **侧页（任务/商店）没有指纹 -> 挂机永久卡死** -> `unknown.act()` 加第 0 级逃逸：
   `is_back_arrow()` 量左下角 `(38,950,90,992)` 内青色像素 `(b>170)&(g>140)&(r<150)`，区间 150~1000，
   命中就点 `BACK_ARROW_POS=(63,970)` 返回。实测任务页 463 / 大厅·结算·宝箱面板 0 / 战场页 1860（超上限）。
3. **价格护栏**（`pages/chest_info.py: PRICE_RE`）：月卡弹窗的 `￥68` 在 `(278,788)`，
   和免费开箱按钮 `(278,785)` 只差 3px —— 弹窗一旦被误判成 `chest_info`，旧代码就替玩家花钱。
   现在按钮自身或邻域（±70/±60px）出现 `[￥¥]\d` / `\d+元` / 钻石 / 宝石 / 充值 就拒点改关面板。
   注：**整帧**的紫宝石颜色不能判价（实测色 `(222,62,246)` 在 77/89 帧里都出现，纯 UI 紫），
   但**按钮正下方 33px 窄带**干净：免费 9 帧带内紫点恒 0 / 付费帧 362 —— 所以第 6 条才敢加颜色判据。
4. **弹窗有动画，指纹不能标在动画区**：`vip_month` 旧指纹组在新帧只中 5/15，改标文字行 y538 后 2/2；
   `chest_info` 旧指纹是在"木箱"面板上标的，真机铁箱只中 9/15 -> `pick --label chest_info
   --region 20,130,530,1000 --cluster 40` 重标成单组 8/8（点位落在标题行 y166 + 普稀史传行 y454）。
5. **`battle` 旧指纹 15 个点也全在顶栏 y58~114**（和上面 `lobby` 同一个坑），语料 13 张数值相同 ->
   `verify` 的 13/13 是假绿灯：真机 6 帧实测只中 10/15，日志一直是 `battle ocr 0.90`。补拍
   `shots/battle_live_004258~004314` 6 帧后 `pick --label battle --region 0,130,552,1006 --cluster 40`
   重标成 2 形态（19/19 全中 / 异页误中 0 / margin 0.27 / 覆盖 13+6），最小 y=172 已离开顶栏。
6. **⚠ 机器人真的花掉过 30 紫宝石**（00:57 那轮，顶栏 `246/114/1037` -> `251/84/1042`）：宝箱
   **解锁后计时中**再点同一格，面板按钮变成 `开启` + "紫宝石图标 + 30"，OCR 只读得到裸数字 `30`
   （没有单位）-> 第 3 条的 `PRICE_RE` 一条都不命中，被当成免费按钮点了。上一轮"紫宝石 -29 是玩家
   手动"**结论作废**：那 -29 = 花 30 宝石开箱 + 到账 1 宝石，是同一个坑。现在补两道判据：
   `PRICE_BARE_RE`（按钮**下方**的纯数字 = 价格）+ `gem_cost_pixels()`（按钮正下方窄带内紫宝石像素
   >= 60）。真机付费帧离线回放已拒点（`scratch\scripts\test_chest_paid.py` 11/11），
   `test_price_guard.py` 扩到 21 项锁死。01:07 复跑：顶栏 `251/84/1042` -> `256/84/1047`，紫宝石 84 未动。

另外实测确认（省得再走弯路）：
- **大厅里宝箱转好后直接点格子 = 跳过信息面板直接开箱**（`lobby -> chest_open`），只有"点击解锁"态才会进 `chest_info`。
- **解锁（免费启动倒计时）和开箱本身都不消耗顶栏资源**：00:42 那轮全程顶栏 `246/114/1037` 一字未变；开箱到账 +5/+5；01:07 复核同样只有 +5/+5。**唯一会花钱的是"计时中的格子被点成开启"，见第 6 条**。
- 战斗页顶栏那行 `Diamond` 是**对手的玩家昵称**（红方），不是货币，别拿它做判据。

### 2026-09-02 真机实测后修的三处（用户在场实测）

1. **`lobby` 指纹重标定**：旧指纹 15 个点全落在顶栏金币/钻石数字上，语料 21 张恰好数值相同 ->
   `verify` 70/70 是假绿灯，真机只剩 11/15、20 轮路由零命中。改用
   `pick --label lobby --region 0,130,552,1006` 重标（25/25 全中 / 异页误中 0 / margin 0.20），
   并把 4 张真机帧（`shots/live_20260902_a~d`）补进语料（70 -> 74）。
2. **窗口尺寸对齐**（`game_utils.set_window_size()` + `App.ensure_window_size()`）：真机原窗口 431x788
   下指纹只剩 23/60，钉回 552x1006 才 60/60。点击坐标不参与缩放换算，所以必须钉尺寸。
3. **OCR 同分 tie-break**（`pages/base.py: detect_ocr`）：大厅常驻"月卡"和 `vip_popup` 关键词同分 1.0，
   按注册顺序会误判成弹窗；现在并列时用点色部分命中率拆伙（缩放帧实测 lobby 0.80~0.93 / vip 0.00）。

### 2026-08-31 修掉的真 bug

`ctx.clicked_cells` 只在 `auto_bot.py __init__` 建过一次，全项目没有一处清空，
而 `pages/battle.py` 用 `if (cx//16, cy//16) in ctx.clicked_cells: return False` 跳点过的格子；
格子 key 是固定窗口坐标、每场复用 -> 打第 2 场起整张棋盘被永久拉黑，战斗页不再动手。
现在进入 battle 页时 `clicked_cells.clear()` + `last_cell = 0`。
`test_battle_loop.py` 是这套行为的回归测试：把清空退化掉，它会红 3 项。

### 待办（按优先级）

1. ~~**`lobby` 不要点「计时中」的宝箱槽位**~~  **已于 2026-09-03 改成点色判据**：`chest_states()` 只看按钮行
   （y838..882）有没有金色/白字块，计时中的槽位那一行根本没有按钮 -> 判成 `.` -> 不点；
   残留：`.` 分不清「计时中」和「真空槽」，但两者**都不该点**，行为正确。付费面板的护栏仍留在 `chest_info`。
   ✅ **2026-09-03 晚再补一刀**：原来 `u`（白字[点击解锁]）是个**假档**，把免费解锁和花宝石开箱混在一起 ->
   现已按右上角红角标拆成 `U`(免费, 点) / `p`(要宝石, 不点)，596 观测两簇零骑墙 + 43 张面板配对零反例，
   真机第 21 轮点中免费解锁、第 22 轮零白跑（`test_chest_unlock.py`，见 `COLORPRINT.md` §23）。
   🔴 **2026-09-03 深夜再改口径（用户：看到[点击解锁]就点宝箱）**：一格免费都没有时**试探点一格 `p`**，
   点还是关由 `chest_info` 的宝石像素硬闸判 —— 真机第 26 轮 2/2 帧实测 `p` 面板 = 362（付费）→ 只关面板 + 拉黑，
   顶栏紫宝石 303 → 306（一分没花）。`p` 的直接证据补齐，见 §26 / README 第 25+26 轮小节。
   ✅ **第 27 轮（用户截图同一形态 `ppap`）再复验**：2/2 帧试探 → 面板 366/362 → 关面板拉黑，宝石 306 → 307；
   并且按用户截图里的[空槽位]逐槽审计过：空槽位卡身棕色像素恒为 0、白字在按钮带上方 28px，
   判据读出来就是 `.`，**试探分支永远碰不到它**（见 §28 / README 第 27 轮小节）。
2. **补 `claim_popup` / `diamond_popup` 完整截图并标定指纹**（**判页**仍只有 OCR 关键词；不过**动作层本轮已零 OCR** ——
   两页的默认出口都换成点色 `find_close_badge()`，认不出就什么都不点，所以这两条 OCR 判页
   **已经不在关键路径上了**（旧版"认不出就盲点 (270,300) 遮罩 / 靠 `chest_open` 点色按钮兜底"都已删）。
   补到语料仍是第一优先，见 `COLORPRINT.md` §8 / §20。
   ~~`ad_popup`~~ **本轮（§19）优先级下调**：看广告期间主循环由「看广告窗口」接管、**根本不定页**，
   广告页的出口也已经换成点色几何判据（`ad_close_pos()` / `ad_pill_right()`）；
   `ad_popup` 的 OCR 关键词现在只剩「窗口外撞见野生广告页」和「认挽留框里的[继续观看]」两处用得到。
   ~~注：`chest_info` 动作层仍要 ROI OCR 认价格文字~~ **已于 2026-09-03 凌晨解决**：面板价格不看文字，只看按钮内下半那条带的紫宝石像素数（纯点色），`chest_info` 也声明了 `act_needs_ocr = False`（见 `COLORPRINT.md` §12）。
   🔴 **`config.WATCH_ADS = True` 只管结算页那颗黄色[领取]**（`pages/result.py` 是全项目唯一读它的地方）。
   大厅 `a` 格 [[AD]-30分钟加速] 与 `chest_info` 的 [AD] 入口**仍然一律不点** —— 不是忘了改：点下去之后是什么
   面板从来没采集过（19:11 那批黑帧只证明了"会进广告"），而广告额度一天只有 8 次，白试一次就烧掉一次。
   **明天额度重置后的第一件事**：单发探针只点一次 `a` 格，把「面板 -> 广告 -> 回到游戏」整条链的帧抓下来
   （顺手第一次真机验证 §22 药丸尺子和 §18 黑屏出口），有证据再接进 `lobby.act()`。**别照着开关名把 `a` 直接放开。**
3. **单帧形态没有跨帧验证**：`matching` 全部、`result` 第 4 形态、`vip_popup` 的 `vip_month`
   （详见 `COLORPRINT.md` §9-1），补图优先级 `sm_after` / `st_2`。
4. **`battle` 真机形态只补到 6 帧**（`shots/battle_live_*`）。换对手昵称/换矿型仍可能出现第三形态；
   `test_print_route.py` 第 [7] 项目前只硬卡 `lobby`，`battle` 靠 margin 0.16 自证，继续补帧更稳。
   （原第 1 项"battle 指纹全在顶栏"已于 2026-09-03 重标解决，见上面第 5 条。）
5. **`versus`（对战列表页）是凌晨真机新加的页，语料只有 4 帧**，判色点手工标定，换赛季 / 换活动皮肤后可能整片失效 -> 继续补帧复核。
   它与 `matching` 的**单向重叠已判定刻意不修**：4 张 `matching` 帧对 `versus` 指纹也是 1.00 全中（反向只 0.30），但 `matching` 注册在前、且两页 `act()` 都 `return False` -> **没有任何行为后果**。要拆开只能标在「匹配成功!」字样或 VS 光球上，那是动画区，比现在更不稳。
6. **`levelup`(4 帧) / `hero_level`(5 帧) 语料很薄，而且全部来自同一个等级状态**：指纹刻意只挑"与等级/奖励无关"
   的恒定区域（标题字样 / 蓝横幅带），所以升级本身不会让它失效；但若哪天[领取]按钮上多出"看广告双倍"
   （黄块像素数会冲破 `CLAIM_MAX_PX`）或横幅整体位移，动作层会**自动不动**（安全侧失败），
   届时必须补帧重标 —— 用 `scratch/scripts/compose_hero.py` 那套单元级口径，别再用点级 margin。


7. ~~**`vip_popup` 判页仍会走一次 OCR**~~ —— **2026-09-03 中午已解决**：那张面板会整体浮动，绝对点色必然漂。
   改为**锚点相对点色**（`anchor_prints`：先定徽章，再按固定偏移读金币排），只允许全中。
   真机 12:31 那一段 8 条路由全部 `print-order`/`print-prefer`，**走 OCR 的路由 = 0 条**。见 `COLORPRINT.md` §6.2。
8. ~~**`shots/` 里 12 张帧没进 `LABELS`**~~ —— **已解决（2026-09-03 午后）**：那 14 张真机取证帧（13 张 `stuck_arrow_*` +
   1 张 `stuck_guide_102410.png`）已由 `pick_print.py` **自动登记进 `other`**，`check` 已归零。
   踩过的坑：去重必须按**所有**标签判 —— `stuck_01*` 那 6 张早就登记在 `chest_info` 里，只按 `other` 判会重复登记。
   **根因也一起堵了**：`unknown` 的取证帧离线不再写 `shots/`，改走 `stuck_dir(ctx)` -> `scratch/reg_stuck/`（§20.3）。
   同批新增的 `ad_popup_live122400.png` **刻意归 `other`**：广告 chrome 是白字黑底，标指纹容易在别的黑底页上误中，
   出口判据走 §18 的几何点色更稳。
9. **锚点点色（`anchor_prints`）目前只有 `vip_popup` 在用，真机实证 3 帧（10:56 一次 + 第 19 轮 16:43:13 / 16:46:23 两次 `vip_popup print-anchor 1.00`）**。`claim_popup` / `diamond_popup` /
    `ad_popup` 三页判页仍只有 OCR 关键词（**本轮 §19 之后 `ad_popup` 已不在关键路径上**，见待办 2），等采到语料可以照 §6.2 那套搬过去（`anchor_snap_0903.py` 是现成的采集脚本）。
     第 20 轮（17:19:33 / 17:30:56）又响 2 次 -> 真机实证从 3 帧涨到 **5 帧**（§22.5）。

10. **看广告窗口的已知软肋**（§19，都有兜底、不会卡死）：
    (a) ~~「放完了」只有药丸**相对缩窄**一把尺子~~ —— **本轮已加第二把**：`ad_pill_state()` 同时数带内白像素总数，
        比本场峰值少 >=15% 且连续 4s 也算放完（§19.3.1）；外加**顶栏门闩**（左边界漂 >20px 该帧不许作数，§19.3.2）。
        残留：某家 SDK 若放完时**既不改文案、也不改字数**，仍然只能等满 `AD_WATCH_MAX = 40s`
        -> 对超过 40s 的广告会提前点、奖励作废（行为安全，但白看）。真机目前 7/7 都是"缩窄"先响，两把新尺子还没见过世面。
    (b) `ad_pill_right()` 在**正常页也会返回 176~329**（顶栏任何白字都算），所以它**只在 `ad_close_pos()` 命中时
        才允许调用**，绝不能当独立判据用 —— 新加同类「读 chrome」的尺子时要照这条写前置条件。

11. ~~**弹窗关闭徽章这条出口真机 0 次命中，只有回归锁着**~~ —— **已实证（2026-09-03 15:52:57）**：
    真机第 18 轮 `[指纹] unknown escape-badge 1.00` + `点候选 1/4 -> (452,361)` 一次即中，8s 后回大厅。
    而且落点 (452,361) 是**语料里没有的第 4 个高度**（235/224/197 之外），x 也从 465/471 飘到 452
    -> 再次钉死"落点只能现算"。见 `COLORPRINT.md` §20.5。
    第 19 轮又命中 **4 次**（`hero_level` (469,172) + `vip_popup` (455,461)/(459,545)/(452,370)）-> 高度已数到 **7 档**，全部第 1 候选即中、每次 3s 内回大厅。见 §20.2 真机表 / §21.8。
    第 20 轮再命中 **4 次**（(459,545)×2 + (455,461)×2），高度没有新增（仍是 7 档），但**连续两轮 8 次全部第 1 候选即中**，见 §22.5。
12. **`newcard` 语料只有 4 帧，而且全部来自同一张卡（弩炮）**：指纹刻意只取底部两条与卡无关的带
    （白字「点击继续」+ 紫底装饰），卡名/兵种/美术区一律不碰（§21.3），所以换卡不会失效；
    但**若哪天底部装饰整体位移或白字改文案，动作层会不动作（安全侧失败）** -> 届时补帧重标。 真机第 19 轮首命中一次（开局那张就是它，落点 (276,864) 与离线预测逐字一致），但**仍是同一张弩炮卡** —— 换卡不失效目前只有离线证据（15 点逐位色差 0），采到第二张卡再验。
13. ~~**干掉最后一条 OCR 路由**~~ —— **实测之后决定不动**：`shots_live/ocr_result_133258.png`（真结算页、一次动画糊掉了判色点）
    探针量清楚了：`result` 指纹 **25 点 / score 0.800 / missing 5**，5 个缺点**全挤在同一整个十字单元 (386~390, 144~148)**，不是散点抖动；
    登记它再重标 = 逼 result 丢掉那一整个十字（**25 → 20 点**）去迁就一帧动画，比多救一帧亏。
    现状由「软命中 + 零命中防抖」兜住（`SOFT_MAX_MISS = 1` 明确不放行缺 5 点的帧），真机第 19 轮 400 步**一次没犯**。见 `COLORPRINT.md` §20.4。
    第 20 轮 400 步同样**一次没犯**（6 次结算全部 `result print-order 1.00`），见 §22.5。

14. **广告额度重置后的第一次实证**：`ad_claim_pos()` 判据本轮**没改**，但「0 命中 = 额度用完」这个结论目前是
    靠「横幅右下角写着 1/8」+「探针帧整带 0 黄像素」+「胜利/失败共用同一颗药丸」三条间接证据拼出来的（§22.3）。
    次日额度重置后带 `--ad-probe 12` 再跑一轮：第一次进结算页应重新打出
    `[结算] 点色命中[领取](看广告) (412, 782)`；**只有**「探针帧下带里确实有黄像素而判据仍返回 None」
    才需要重标 `AD_CLAIM_W / AD_CLAIM_H / AD_CLAIM_MIN_PX`。

    ✅ **2026-09-04 凌晨已实证到底**：结算页[领取] → 看广告 → 自动关 整条链真机跑通（R34 04:22:11 → 04:22:48，
    33s，零 OCR，药丸缩窄 211->164 判"已获得奖励" → 点[关闭] (488,77)），判据本身**不用重标**。
    同时排除了四条"广告没起来"的误差解释（焦点 / 遮挡 / 坐标 / 额度，见 README 第 28~35 轮小节）：
    真因是**微信激励视频有时没填充**（此时游戏改弹"¥68 跳过激励视频"月卡弹窗，`vip_popup` 路由已兜住）；
    另一个发现：**PrintWindow 抓不到视频层**（真广告帧非黑像素只有 5%，下半屏全黑），
    但药丸 / [关闭] 都在顶栏、判据不受影响；新增第二通道 `game_utils.capture_screen()` 只用于取证。
    🔴 大厅 `a` 格 [[AD]-30分钟加速] **仍未采证**，别照着开关名放开。

## 文件地图

| 路径 | 作用 |
|---|---|
| `auto_bot.py` | 主循环/接线：抓窗 -> `route()` -> `page.act()`；每轮 `ensure_window_size()` 对齐基准尺寸；含 CPU 降载、`--shots` 取证存帧；**看广告窗口** `start_ad_watch()` / `_ad_tick()` 优先于路由（期间不定页、零 OCR、零点击） |
| `config.py` | 每页 ROI/scale/poll/ocr_gap；并暴露 `ROOT`、`SHOTS_DIR` |
| `game_utils.py` | Win32 抓窗口（PrintWindow，后台可用）、找窗口、PostMessage 点击、`set_window_size()`（把窗口钉回指纹基准 552x1006） |
| `vision.py` | OCR 封装（rapidocr + ROI 缩放），`ocr_config.yaml` 是它的模型配置 |
| `colorprint.py` | 点色指纹原语（移植自 `..\mxdzz\libs\app.py` 的颜色对比部分） |
| `pages/base.py` | `Page` 基类 + `match_print()`（**四级路由**：绝对全中 -> 锚点相对点色 -> 软命中 -> 全图 OCR）/ `detect_ocr()` / `route()` / `route_prints()`，以及按颜色判据 `find_close_badge()` / `is_back_arrow()` / `is_countdown()` / `is_transition()`（认转场的白烟/黑屏帧，整帧跳过不花 OCR）/ `ad_close_pos()`（黑屏赖着不走时的广告页[关闭]）/ `ad_claim_pos()`（结算页黄色[领取] = 看广告）/ `ad_pill_right()`（广告状态药丸右边界，判"放完了没有"）/ `nav_present()` / `guide_targets()` |
| `pages/*.py` | 16 个页面：11 个已标指纹（lobby/battle/result/chest_info/chest_open/vip_popup/matching/versus/**levelup**/**hero_level**/**newcard**），3 个只走 OCR 判页（ad_popup/claim_popup/diamond_popup；广告页那张"放完的整黑尾帧"由 `ad_close_pos()` 出口处理，而且**看广告期间主循环由 §19 窗口接管、根本不定页**，`ad_popup.act()` 现在的职责是「弹挽留框时点[继续观看]」），`unknown` 兜底，**`tab_other` 刻意不标指纹**（兄弟页签只能靠"导航栏在不在 + 亮的是哪个"定身份，§16）；**15 个页**（除 `ad_popup` 外全部）声明 `act_needs_ocr = False`，动作层也只数颜色 |
| `pages/unknown.py` | 兜底页：候选出口按「引导落点 -> **弹窗关闭徽章** -> 青色返回箭头 -> 导航栏兄弟页签 -> 弹窗遮罩」排队；`stuck_dir(ctx)` 决定取证帧写哪 —— **真机写 `shots/`，离线回归写 `scratch/reg_stuck/`**（不再污染语料）；同帧最多点 `MAX_TRY = 5` 次，画面签名零变化就 `_give_up()` 存帧 + 睡 45s |
| `test_*.py` | 离线回归（**18 个全绿**）：`test_print_route`(10 项/261 语料) `test_zero_ocr`(定页零 OCR) `test_act_zero_ocr`(动作层零 OCR + 逐帧落点表) `test_cpu_offline`(主循环 8 场景) `test_battle_loop`(连打 3 场) `test_price_guard`(39 项不花钱护栏) `test_chest_livelock`(开箱死循环) **`test_side_page_escape`(引导模态/兄弟页签逃逸 6 场景，全程零 OCR)** **`test_price_class`(战斗价签分类)** **`test_battle_rank`(档位出手次序+不停手+白标签判据防底色漂移 §34)** **`test_anchor_print`(锚点相对点色)** **`test_ad_escape`(广告页自救)** **`test_ad_watch`(看广告自动领奖 4 段：[领取]判据 / 药丸两把尺子+门闩 / 状态机 / 端到端零 OCR)** **`test_popup_escape`(弹窗关闭徽章出口 5 段：四判据交叉 / step() 真跑零 OCR / claim·diamond 出口 / 828 帧"谁都不认"<=3)**
**`test_newcard`(新卡展示页 6 段：真机 4 帧硬命中 / 15 点逐位色差 0 / 异页零点击 / step() 端到端零 OCR / 830 帧唯一命中 / `_flag_stuck` 盲区复现)**
 **`test_chest_unlock`(宝箱五档码表 5 段：596 观测角标两簇零骑墙 / 码表自洽 / 43 张面板配对零反例 / p 格一律不点的行为 / 真机帧双向锁)** **`test_fps_popup`(帧率自适应弹窗 7 段：10 帧硬命中 / 15 点全在面板内 / act 落点 / step() 零 OCR / 2764 帧唯一命中 / 回执键三态 / 额度扣减)** **`test_dim_frame`(压暗闸门 5 段：现场帧 battle 0/15 + 不许被任何页认领 + 590 次判定误判 0)** |
| `battle_scan.py` | 战斗回合的颜色扫描（避免整帧 OCR，省 CPU）：`white_mask()` 用**局部对比**而不是绝对亮度，浅色地图才不会把整块棋盘吞成一块（§34）；单帧 40.6 ms（含 15x15 中值滤波）。`battle_templates.npz` 是旧模板匹配遗留，**现在没有任何代码引用**，可删可进 gitignore |
| `shots/` | **261 张**标定语料（552x1006，含 2026-09-02/03 真机帧），`tools/pick_print.py` 的 `LABELS` 引用它（**不入库**）；真机取证帧已全部登记（14 张 `stuck_*` 归 `other`），`check` 已归零 |
| `shots_live/` | 真机跑起来的取证帧（`peek_*` 只读探针 / `dbg_*` 主循环 `--shots`），**不入库** |
| `tools/` | `pick_print.py`(check/pick/verify) `print_stats.py` `live_audit.py`(真机证据帧全量复核) `set_low_cpu.ps1` + 逆向工具（il2cppdumper、wxapkg） |
| `capture/ dec/ game_src/ unity_data/` | 逆向资料：抓包、XYX 解密产物、wasm 解包、Unity 资源（**运行时不依赖，不入库**） |
| `scratch/` | 一次性脚本/中间产物归档，带 `MANIFEST.tsv` + `restore.py`（把归档搬回原位）+ `relocate.py`，**运行时不依赖**；`scratch/reg_stuck/` = **离线回归**留下的 `stuck_*.png` 取证帧（每跑一次回归就会刷新几张，绝不进语料） |
| `COLORPRINT.md` | 点色指纹那层的完整说明：怎么标定、怎么加新页、和 mxdzz 的差异 |
| `AUTOMATION_REPORT.md` | 逆向结论报告（API 路线为什么放弃、已破解到哪一步）（**不入库**） |

## 想加新页面 / 新指纹

看 `COLORPRINT.md` §5/§8。一句话：截图丢进 `shots/` -> 在 `tools/pick_print.py` 的 `LABELS` 登记
-> `pick --label <页名> --region 0,130,552,1006` 采指纹（**必须排掉顶栏的账号资源数字**，§9-6）
-> 贴进 `pages/<页名>.py` -> `verify` + `test_print_route.py` 复验。
**弹窗页必须注册在主页面前面**，`ALL_PAGES[-1]` 必须是 `unknown`。
两类页型不用标绝对指纹：
- **会浮动的弹窗面板**（`vip_popup` 那种）：绝对点色一漂就全不中 -> 声明 `anchor_prints`，
  先按锚点（徽章/标题块）定住面板，再按**固定偏移**读点色，见 `COLORPRINT.md` §6.2；
- **版面整片变的页**（放完的激励视频广告页那种整黑）：没有稳定像素可标 -> 标进 `LABELS['other']`
  并要求"任何指纹都不许中它"，出口用**几何点色判据**（`ad_close_pos()`：顶栏右半白团块 + 整页必须黑），见 §18。
