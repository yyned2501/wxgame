# -*- coding: utf-8 -*-
"""结算页: 看广告领奖(失败/胜利礼包, 纯点色) -> 点击继续"""
import logging
import time

from config import WATCH_ADS

from .base import Page, ad_claim_pos, color_button

class ResultPage(Page):
    name = 'result'
    next_pages = ('lobby', 'claim_popup', 'ad_popup', 'vip_popup', 'matching',
                  'levelup', 'hero_level', 'unknown')
    # 点色指纹: 由 tools/pick_print.py pick --label result 自动标定(勿手改)
    # 2 种形态 x 5 个十字单元(每单元 5 点, 共 25 判色点/形态), 任一形态【全中】即判为 result。
    #   形态 A(25 点) 失败页 x 聚光灯「亮相位」: 语料 23/35 帧全中。
    #   形态 B(25 点) 王座厅四角纯背景: 语料 35/35 帧全中。
    #   复核(2026-09-05 删掉违规右中带点后): 语料 35/35 全中 / 异页误中 0 / margin 0.36(异页最高只中 9/25 点)。
    # 🔴 为什么必须补形态 B(2026-09-04 真机第 30~31 轮定案): 结算页背景 = 大厅王座厅 + 一束会呼吸
    #    的聚光灯光芒, 光强几十秒内从「整屏发白」走到「角落近黑」(实测同一点 #412734 -> #0E1025,
    #    逐通道差 51/23/15, 而 degree85 只容 +/-19) -> 01:45 之后每一张 result 帧都只中 2/25,
    #    整条路由退化成全项目唯一一条 OCR 路(每帧多花 ~700ms + 存一张待标语料)。
    #    结论: 结算页的指纹点只能落在【光晕半径之外】的画面四角, 见 COLORPRINT.md §29。
    # 🔴 unit0 原来在 (388,146) —— 那是 MVP 页上方的 3D 获胜角色动画精灵(红龙/骷髅…还会闪 ✨),
    #    角色一换就掉分; 2026-09-03 20:14 真机第 24 轮那帧被红龙 + 一颗大闪光正好盖住, 整条只中
    #    23/25 -> 教训见 COLORPRINT.md §5: 动画精灵/奖励图标/数字上的点都不能进指纹。
    prints = (
        (   # 形态 A: 失败页 x 聚光灯亮相位(23/35 帧)
        [76, 162, 0x412734], [78, 162, 0x412734], [74, 162, 0x3F2532],
        [76, 164, 0x412734], [76, 160, 0x412632], [472, 158, 0x422632],
        [474, 158, 0x422632], [470, 158, 0x301B20], [472, 160, 0x412632],
        [472, 156, 0x402530], [500, 242, 0x3C2328], [502, 242, 0x3B2126],
        [498, 242, 0x3A2025], [500, 244, 0x3C2227], [500, 240, 0x3C2328],
        [44, 242, 0x3B2227], [46, 242, 0x3B2227], [42, 242, 0x3B2227],
        [44, 244, 0x3B2227], [44, 240, 0x3C2327], [26, 192, 0x2D1613],
        [28, 192, 0x2D1613], [24, 192, 0x2D1613], [26, 194, 0x2D1613],
        [26, 190, 0x2D1613],
        ),
        (   # 形态 B: 王座厅四角边缘(35/35 帧, 胜/败 x 亮/暗通吃)
        [36, 108, 0x2E1927], [38, 108, 0x2E1928], [34, 108, 0x2E1928],
        [36, 110, 0x2E1927], [36, 106, 0x2E1826], [516, 104, 0x2E1827],
        [518, 104, 0x2E1827], [514, 104, 0x2F1827], [516, 106, 0x2D1827],
        [516, 102, 0x2F1929], [24, 216, 0x311D25], [26, 216, 0x331D27],
        [22, 216, 0x2E1B24], [24, 218, 0x2F1C24], [24, 214, 0x2F1C24],
        [524, 216, 0x2E1924], [526, 216, 0x2C1A24], [522, 216, 0x2D1821],
        [524, 218, 0x2F1A21], [524, 214, 0x2C1824],
        [24, 380, 0x26141E], [26, 380, 0x261414],
        [22, 380, 0x1B121E], [24, 382, 0x1C1420], [24, 378, 0x261418],
        ),
    )
    # 🔴 2026-09-05: 形态 B 原有一个十字单元 (438~442, 374~378) —— 它压在 MVP 横幅右侧
    #    ✨闪光星的轨迹上, 真机两帧(ocr_result_161544 / 125327)被星整单元打掉 ->
    #    26/30 = 0.867, 软命中只许差 1 点 -> 白烧一次全图 OCR, 还把 popup_escape[5]
    #    "谁都不认"上界顶穿(3->4)。按本文件头 §29 定案"指纹点只能落在光晕半径之外的
    #    四角", 这个右中带点本来就是违规的; 换点方案实测(右侧带 8 候选全部异页单元素
    #    中 423~444 帧 = 王座暗红墙色太通用, 且闪光轨迹覆盖整条右带), 所以**删掉降为 25 点**,
    #    两帧病灶帧回归 print-order 1.00。margin 复核实测见 tools/print_stats.py。
    # ---- 动作层点色(2026-09-03 定案: 零 OCR) --------------------------------
    # 陷阱: watch_124707 是激励视频盖在结算页上的帧, 扫描 n=0 -> 本帧不动作。
    #   旧版在这里靠 OCR 找"点击继续", 读到遮罩上的广告文案就瞎点一次。
    # 标定只认真机现采帧(2026-09-09 14:46~14:49 --shots, 历史截图一律不作数):
    #   dbg_000(无横幅)/dbg_051/dbg_104(有横幅) 三帧, 度90 下"点击继续"都是白字
    #   1293~1431 px -> 稳定命中 (275,900); 同框内青色 0x88E4FF 只 2~3 px = 根本没有。
    # 框边界也是量出来的: 底排 y[879,913] 的白字横跨 x[51,480], 混着左卡牌[+15]/中间
    #   [点击继续](x[211,324])/右部[可用 2/8](x[405,480])三处 -> x 收到 [150,400] 只剩中间那个;
    #   y0=870 压住上一条白字带 y[819,833]。MIN_PX 600 落在 1293 与 0 之间。
    CONTINUE_BOX = (150, 870, 400, 960)
    CONTINUE_COLOR = 0xFFFFFF
    CONTINUE_MIN_PX = 600
    act_needs_ocr = False   # 纯点色页: 主循环不为本页跑 OCR
    # [领取]点完到下一次点它的间隔. 领完广告退回结算页时横幅往往还在(那是"可用 8/8"的每日额度),
    # 不设节流就会在同一次结算里反复开广告。120s > 一场战斗 + 一条广告, 等于"每场最多领一次"。
    CLAIM_GAP = 120.0
    # 点完[领取]后压住[继续]的秒数(仅当黄色横幅【还在】时才压): 广告最迟 12s 才盖上黑屏
    # (真机 08-24 12:47 实测), 这期间结算页还在 -> 手快点[继续]会把它关掉、广告作废。
    # 横幅一消失就说明领取已到账, 不必等满, 立刻走[继续](用户 2026-09-09 定案"不干等")。
    # 会员更是不弹广告: 14:20:52 点领取, 0.3s 后已在大厅撒钻石。
    CLAIM_GRACE = 12.0
    # 额度是 8 次/天: 2026-09-03 13:32:58 那一帧横幅上写着"可用 1/8", 领完这一次之后
    # 第 18~20 轮共 1000+ 步再没见过横幅(探针帧整个下带 0 个黄色像素) -> COLORPRINT.md §22.3。
    _claim_until = 0.0      # [领取]后的宽限截止时刻(见 CLAIM_GRACE)

    def detect(self, f):
        # x0.5 下"继续/维续"易读错, 多做几个容错
        if f.has('点击继续', '点击维续', '再来一局'):
            return 0.95
        if f.has('失败', '胜利') and f.has('MVP', '奖杯', '经验', '点击', '维续'):
            return 0.8
        return 0.0

    def act(self, ctx):
        now = time.time()
        img = ctx.f.img
        # 2026-09-11 用户反馈 result 卡很久: 之前"先认黄色[ad_claim_pos] -> 没广告 -> 4s 撒手"流程
        # 在广告额度用完时白白浪费一次战斗(点了黄色但 SDK 不发量, bot 还得等 4s 撒手).
        # 现在: 紫色[点击继续]永远在画面里(底排固定), 优先认紫色; 命中就直接走, 省一次 4s 等待
        # 和广告额度消耗. 紫色没命中(可能是动画帧/某种遮挡)再退回去尝试黄色[领取].
        continue_pos = color_button(img, self.CONTINUE_BOX, self.CONTINUE_COLOR,
                                    min_px=self.CONTINUE_MIN_PX)
        if continue_pos is not None and not ctx.acted('result_continue'):
            ctx.battles += 1
            logging.info(f'[结算] 点色命中继续按钮 {continue_pos} (累计 {ctx.battles} 场)')
            ctx.click(*continue_pos)
            return True
        # 紫色没命中(动画中/被遮), 退回去看黄色[领取]. 用户想看广告 + 还有额度才会走这条.
        banner = ad_claim_pos(img) if WATCH_ADS else None
        if banner is not None and not ctx.acted('result_claim', self.CLAIM_GAP):
            logging.info(f'[结算] 点色命中[领取](看广告) {banner}')
            # 抢前台必须在点之前: 微信是在"点下去那一刻"决定给不给量的。
            # 真机 2026-09-04 凌晨后台点了 6 次[领取]一条广告都没起来, 而 08-24
            # 12:47 用户在场(窗口在前台)那一次 12s 后真的盖上黑屏广告 -> config.AD_FOCUS。
            fg = getattr(ctx, 'ensure_foreground', None)
            if fg:
                fg('结算礼包[领取]')
            ctx.click(*banner)
            self._claim_until = now + self.CLAIM_GRACE
            return True
        elif banner is None:
            shot = getattr(ctx, 'ad_probe_shot', None)
            if shot is not None:
                shot(img, 'result')
        # 两者都没命中: 可能在动画帧或转场中, 等下一帧
        return False
