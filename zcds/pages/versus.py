# -*- coding: utf-8 -*-
"""匹配成功页(VS 页): 蓝/红对分背景 + 双方阵容卡, 等它自动进战斗, 全程零 OCR

真机 2026-09-03 03:12 定位: 这张页原来没进指纹表 -> 每帧掉全图 OCR(675ms)然后判成 unknown,
     unknown 只会点遮罩 —— 在 VS 页上点遮罩没有任何意义, 白等几秒还污染日志。
指纹取点原则(只标在"不会因对手变化"的地方):
  * 左右两条竖带(x=40 / x=515): 蓝方/红方渐变底 + 六边形格纹, 与对手阵容/段位无关;
    中央(匹配成功! 字样、VS 光球、斜向闪电)和两张卡内部(战力数字、英雄头像)一律避开;
  * 底部 y=950 也在竖带上 —— "提示"轮播条只在 x 70~480, 碰不到这两列;
  * 背景是平滑渐变, 十字 5 点颜色几乎相同 -> 等于给这一列放宽了 1px 抖动, 稳。

和 matching 的已知重叠(2026-09-03 复核, 结论=不用修):
  4 张 matching 语料帧对本页指纹也是 1.00 全中(两页共用同一套蓝/红竖带背景),
  反向不成立(4 张 versus 帧对 matching 指纹只有 0.30)。因为
    1) matching 注册在 versus 之前, 平票时永远是 matching 赢;
    2) 两页 act() 都是 `return False`(只等游戏自己往下走), 判成谁都点不出东西;
  所以这个重叠没有任何行为后果。真要拆开就得在"匹配成功!字样/VS 光球"上标点,
  那正好是动画区, 反而更不稳。
"""
from .base import Page


class VersusPage(Page):
    name = 'versus'
    next_pages = ('battle', 'matching', 'unknown')
    act_needs_ocr = False     # 本页只等待(游戏自己进战斗), 永远不需要 OCR
    # 点色指纹: 手工标定(取点原则见模块 docstring), 2026-09-03 起有 4 帧真机语料
    # 1 种形态 x 6 个十字单元(每单元 5 点, 共 30 判色点/形态), 任一形态全中即判为 versus; 语料 4/4 全中 / 异页误中 4 / margin 1.00(异页最高只中 30/30 点) / 各形态覆盖 [4] 帧
    points = [
        [40, 120, 0x0E2CA5], [42, 120, 0x0E2CA5], [38, 120, 0x0E2CA5],
        [40, 122, 0x0E2CA5], [40, 118, 0x0E2CA5],
        [40, 480, 0x3B75EA], [42, 480, 0x3B75EA], [38, 480, 0x3B75E9],
        [40, 482, 0x3B74EA], [40, 478, 0x3B75EA],
        [40, 780, 0x204BC1], [42, 780, 0x204BC0], [38, 780, 0x1F49C0],
        [40, 782, 0x204CC1], [40, 778, 0x204BC0],
        [515, 120, 0x882C33], [517, 120, 0x872C32], [513, 120, 0x862B31],
        [515, 122, 0x882C33], [515, 118, 0x8A2D33],
        [515, 480, 0xBB4A41], [517, 480, 0xBC4A41], [513, 480, 0xBB4A41],
        [515, 482, 0xC24C42], [515, 478, 0xBF4C42],
        [515, 950, 0x812A31], [517, 950, 0x812930], [513, 950, 0x812931],
        [515, 952, 0x832931], [515, 948, 0x802930],
    ]

    def detect(self, f):
        # OCR 只作兜底(指纹没全中时才会走到这里); 动作层不依赖它
        return 0.9 if f.has('匹配成功', '匹配成动') else (0.6 if f.has('提示') else 0.0)

    def act(self, ctx):
        return False        # 只等待: 停留 ~2s 后游戏自动进入 battle
